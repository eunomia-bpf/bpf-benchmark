# Phase 3 BBProgram strict fix, 2026-05-11

## Task 1: VerifierOracle is InsnSite-keyed

- `bpfopt/crates/bpfopt/src/analysis/bbprogram.rs:13` defines `VerifierOracle` as a `BTreeMap<InsnSite, Arc<[VerifierInsn]>>`, preserving duplicate verifier visits/frames per site.
- `bpfopt/crates/bpfopt/src/analysis/bbprogram.rs:34` stores the lifted site-keyed oracle in `BBProgram`.
- `bpfopt/crates/bpfopt/src/analysis/bbprogram.rs:267` adds `BBProgram::oracle_at(site) -> Option<&VerifierInsn>`.
- `bpfopt/crates/bpfopt/src/analysis/bbprogram.rs:270` keeps crate-internal multi-visit access for const_prop without exposing PC-indexed storage to passes.
- `bpfopt/crates/bpfopt/src/analysis/bbprogram_lift.rs:117` passes a lifted oracle into `BBProgram::new`.
- `bpfopt/crates/bpfopt/src/analysis/bbprogram_lift.rs:130` maps raw verifier PCs to `InsnSite` during lift.
- `bpfopt/crates/bpfopt/src/passes/wide_mem.rs:428` consumes `prog.oracle_at(start_site)` only.
- `bpfopt/crates/bpfopt/src/passes/const_prop.rs:124` consumes site-keyed verifier records from `BBProgram`, not `prog.oracle()`.

## Task 2: PMU profile is InsnSite-keyed

- `bpfopt/crates/bpfopt/src/pass.rs:164` defines `PmuRecord`.
- `bpfopt/crates/bpfopt/src/analysis/bbprogram.rs:35` stores PMU records by `InsnSite`.
- `bpfopt/crates/bpfopt/src/analysis/bbprogram.rs:273` adds `BBProgram::profile_at(site) -> Option<&PmuRecord>`.
- `bpfopt/crates/bpfopt/src/analysis/bbprogram.rs:276`, `:285`, and `:291` resolve raw PC-keyed annotations/profile input through `original_pc_to_site`.
- `bpfopt/crates/bpfopt/src/passes/prefetch.rs:60` attaches CLI profile input through `BBProgram`.
- `bpfopt/crates/bpfopt/src/passes/prefetch.rs:198` and `:256` consume `prog.profile_at(site)`.
- `bpfopt/crates/bpfopt/src/passes/branch_flip.rs:44`, `:174`, and `:224` consume PMU profile data through `BBProgram` as well.

## Task 3: endian_fusion and extract cross-block skip

- `bpfopt/crates/bpfopt/src/passes/endian.rs:309` uses `prog.sites_in_block(block)` for normal scanning.
- `bpfopt/crates/bpfopt/src/passes/endian.rs:369` checks the fallthrough successor and first site of the next block for cross-block skip diagnostics.
- `bpfopt/crates/bpfopt/src/passes/extract.rs:85` uses `prog.sites_in_block(block)` for normal scanning.
- `bpfopt/crates/bpfopt/src/passes/extract.rs:143` checks the fallthrough successor and first site of the next block for cross-block skip diagnostics.
- `next_site_in_linear_order` is absent from `bpfopt/crates/bpfopt/src`.

## Task 4: prefetch InsnSite diagnostics

- `bpfopt/crates/bpfopt/src/passes/prefetch.rs:159` records insertion rejection diagnostics as `InsnSite`.
- `bpfopt/crates/bpfopt/src/passes/prefetch.rs:173` emits `SkipReason.pc` only at report emission via `prog.site_current_pc(site)`.
- `bpfopt/crates/bpfopt/src/passes/prefetch.rs:208` carries candidates as target/insert `InsnSite`.
- `bpfopt/crates/bpfopt/src/passes/prefetch.rs:554` and `:575` return rejection sites instead of formatted PC strings.

## New BBProgram APIs

- `oracle_at(site)`: `bpfopt/crates/bpfopt/src/analysis/bbprogram.rs:267`
- `profile_at(site)`: `bpfopt/crates/bpfopt/src/analysis/bbprogram.rs:273`
- `original_pc_to_site(pc)`: `bpfopt/crates/bpfopt/src/analysis/bbprogram.rs:390`
- `site_current_pc(site)`: `bpfopt/crates/bpfopt/src/analysis/bbprogram.rs:395`

## Lift changes

- Verifier oracle PC to `InsnSite`: `bpfopt/crates/bpfopt/src/analysis/bbprogram_lift.rs:130`
- PMU profile PC to `InsnSite`: `bpfopt/crates/bpfopt/src/analysis/bbprogram.rs:301`

## Verification

- `cargo build --release --manifest-path bpfopt/Cargo.toml`: pass
- `cargo test --release --manifest-path bpfopt/Cargo.toml`: pass, 217 tests
- `cargo clippy --release --manifest-path bpfopt/Cargo.toml --all-targets -- -D dead_code -D warnings`: pass
- `target/release/bpfopt list-passes`: pass, 15 passes listed
- DCE smoke command: pass, exit `0`
- Non-test LOC: `18994 total`

## rg gates

- `rg "oracle\\[" bpfopt/crates/bpfopt/src/passes/`: no matches
- `rg "\\.oracle\\.|\\.oracle\\b" bpfopt/crates/bpfopt/src/passes/`: no matches
- `rg "next_site_in_linear_order" bpfopt/crates/bpfopt/src/`: no matches
- `rg "as usize.*pc\\b|pc:.*usize" bpfopt/crates/bpfopt/src/passes/`: no matches

## Final pass ratings

All 15 passes are A: `noop`, `map_inline`, `const_prop`, `dce`, `skb_load_bytes_spec`, `bounds_check_merge`, `wide_mem`, `bulk_memory`, `rotate`, `cond_select`, `ccmp`, `extract`, `endian_fusion`, `branch_flip`, `prefetch`.
