# Phase 3 API Surface Fix - 2026-05-11

## Deletes

- Deleted `bpfopt/crates/bpfopt/src/analysis/bbprogram_helpers.rs` entirely.
- Removed `mod bbprogram_helpers` and helper re-exports from `bpfopt/crates/bpfopt/src/analysis/mod.rs`; current module list is at `mod.rs:4`.
- Deleted public raw verifier/profile methods from `bbprogram.rs`: `oracle_at(...)` and `profile_at(...)`.
- Deleted public slot/PC methods from `bbprogram.rs`: `block_slot_len`, `block_slot_bounds`, `block_range_for_slots`, `site_for_block_slot`, `sites_in_block_slot_range`, `frame_relative_slot`, `frame_slot_bounds`, and `program_slot_len`.
- Deleted forbidden adjacency helpers from `bbprogram.rs`: `next_site_in_block`, `is_immediately_followed_by`, `adjacent_pairs_in_block`, and `adjacent_windows_in_block`.
- Deleted old public `replace_range(block, Range<usize>, replacement)` from `bbprogram_api.rs`; replacement is `replace_range_at` at `bbprogram_api.rs:49`.

## Visibility

- Identity fields are opaque: `BlockId(pub(crate) usize)` at `bbprogram.rs:19`, `FrameId(pub(crate) usize)` at `bbprogram.rs:21`, and `InsnSite` fields at `bbprogram.rs:23`.
- `VerifierOracle` and `BtfMetadataMap` are crate-visible at `bbprogram.rs:16` and re-exported crate-visible at `analysis/mod.rs:11`.
- Raw pass-facing types are crate-visible at `pass.rs:21`, `pass.rs:190`, and `pass.rs:196`.
- BBProgram internals are crate-visible: `oracle` `bbprogram.rs:275`, `original_pc` `bbprogram.rs:492`, `original_pc_to_site` `bbprogram.rs:498`, `site_current_pc` `bbprogram.rs:503`, `block_start_pc` `bbprogram.rs:561`, remap helpers `bbprogram.rs:616`/`629`, `insn_slot_width` `bbprogram.rs:643`, `attach_side_inputs` `bbprogram.rs:687`, and `reset_btf_to_current_pcs` `bbprogram.rs:703`.
- `VerifierInsn` remains public in `verifier_log.rs` because that file was outside this job's allowed write set; the pass re-export is crate-visible at `pass.rs:21`.

## Adds

- `BBProgram::all_sites(&self) -> impl Iterator<Item = InsnSite> + '_` at `bbprogram.rs:131`.
- `BBProgram::branch_target_entry_sites(&self) -> anyhow::Result<BTreeSet<InsnSite>>` at `bbprogram.rs:509`.
- Typed verifier queries at `bbprogram.rs:281`: `reg_known_constant`, `reg_known_map_ptr`, `reg_proven_bounds`, `reg_kind`, and `site_is_dead_code`.
- Typed profile queries at `bbprogram.rs:323`: `branch_taken_rate`, `branch_miss_rate`, `site_hotness`, and `prefetch_hint`.
- Return types `MapPtr`, `RegKind`, and `PrefetchHint` at `pass.rs:31`, `pass.rs:38`, and `pass.rs:54`.
- `BBProgram::replace_range_at(site, len, replacement)` at `bbprogram_api.rs:49`.
- Pass report boundary: `PassReportSite`, `PassReportPc`, `PassManager`, and `PassManager::finalize_reports(...)` at `pass.rs:384`, `pass.rs:391`, `pass.rs:397`, and `pass.rs:716`.
- CLI report serialization now calls `PassManager::finalize_reports(...)` at `main.rs:1122` and `main.rs:1155`; `main.rs` no longer calls `site_current_pc` directly.
- Added `lift_with_kinsn_registry_and_side_inputs(...)` at `bbprogram_lift.rs:130` so `attach_side_inputs` stays crate-visible while the CLI binary can still enter through a public lift boundary.

## Helper Relocation

- `program_sites(prog)` -> `BBProgram::all_sites()` in `bbprogram.rs:131`.
- `control_flow_target_sites(prog)` -> `BBProgram::branch_target_entry_sites()` in `bbprogram.rs:509`.
- `packet_ctx_layout`, `PacketCtxLayout`, `PacketCtxLayoutScope` -> `insn.rs:87`.
- `SimpleRegValue`, `advance_reg_state`, `advance_alu64_state`, `advance_alu32_state` -> `insn.rs:644`.
- `read_json_file<T>(path, label)` -> private CLI helper at `main.rs:1067`.

## Verification

- `cd bpfopt && cargo build --release`: passed.
- `cd bpfopt && cargo fmt --all`: passed.
- `cd bpfopt && cargo fmt --all --check`: passed.
- Scoped stale API audits over this job's files passed with zero matches for `bbprogram_helpers`, `oracle_at`, `profile_at`, old slot methods, old `replace_range`, and forbidden adjacency helpers.
- Contract greps that are pass-oriented still have definition/internal-boundary matches in API files by design, such as `VerifierInsn` definitions/imports and the single allowed `site_current_pc` call in `PassManager::finalize_reports`.

## Remaining Notes

- Current pass audit residue is test-only: `passes/const_prop_tests.rs` still names `crate::pass::VerifierInsn`, and `passes/map_inline/map_info_tests.rs` uses `InsnSite::for_test(BlockId(0), ...)`.
- No production compile errors remain in the current worktree after the parallel pass updates landed.

## Self Rating

- §2 public API deletions/visibility: Y, except underlying `verifier_log::VerifierInsn` visibility was not changed because `verifier_log.rs` was outside scope.
- §3 identity field visibility: Y.
- §4 pass-facing data type visibility: Y for `pass.rs`/`analysis` re-exports; partial for underlying `VerifierInsn` as noted above.
- §A query additions: Y.
- §B mutation addition: Y.
- §C pass report boundary: Y.
