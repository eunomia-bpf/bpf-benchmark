# AWS ARM64 Test - 2026-04-27

Generated: 2026-04-28 06:24 PDT

## Command

```sh
source /home/yunwei37/workspace/.venv/bin/activate
make aws-arm64-test
```

## Result

PASS. The final `make aws-arm64-test` run exited with status 0.

- Run token: `run.aws-arm64.test.403bdbf4`
- Local results directory: `tests/results/run.aws-arm64.test.403bdbf4/`
- Result file synced: `tests/results/run.aws-arm64.test.403bdbf4/run-contract.json`
- Executor: `aws-ssh`
- Target: `aws-arm64`
- Arch: `arm64`
- Suite: `test`
- EC2 instance type: `t4g.micro`
- AWS profile: `codex-ec2`
- AWS region: `us-east-1`
- EC2 instance: `i-01f5f5491313fde15`
- Remote stage dir: `/var/tmp/bpf-benchmark-aws-arm64/test/run.aws-arm64.test.403bdbf4`

The EC2 instance was terminated after the run. The termination response moved it from `running` to `shutting-down`.

Only `run-contract.json` was synced into the final local results directory. The validation signal for this test run is the successful `make aws-arm64-test` exit status from the AWS executor.

## Rebuilds

The stale ARM64 image tars were removed and rebuilt:

- `.cache/container-images/arm64-katran-artifacts.image.tar`
- `.cache/container-images/arm64-runner-runtime.image.tar`

Final rebuilt artifacts:

- `.cache/container-images/arm64-katran-artifacts.image.tar`: 380,595,712 bytes, 363M, timestamp `2026-04-28 03:33:23 -0700`
- `.cache/container-images/arm64-runner-runtime.image.tar`: 7,535,416,832 bytes, 7.1G, timestamp `2026-04-28 06:06:12 -0700`

## Fixes Applied

The ARM64 build initially failed because the runtime Dockerfile referenced the local Katran artifacts image with `FROM ${KATRAN_IMAGE}`. BuildKit tried to pull `docker.io/bpf-benchmark/katran-artifacts:arm64` from a registry instead of using the locally loaded image.

Changes made:

- `runner/mk/build.mk`: pass Katran artifacts to the runtime build as a named BuildKit context with `--build-context runner-runtime-katran-upstream=docker-image://...`.
- `runner/containers/runner-runtime.Dockerfile`: consume the Katran artifacts through the named external build context and remove `ARG KATRAN_IMAGE`.
- `runner/containers/runner-runtime.Dockerfile`: remove explicit `ARG TARGETPLATFORM=linux/amd64` and `FROM --platform=$TARGETPLATFORM` usage so the CLI `docker build --platform linux/arm64` controls the build platform.
- `runner/containers/katran-artifacts.Dockerfile`: remove explicit `ARG TARGETPLATFORM=linux/amd64` and `FROM --platform=$TARGETPLATFORM` usage for the same reason.

These changes preserved the no-host-bind-mount path and keep app artifacts delivered through image layers.

## Attempts

1. Failed while building the ARM64 runtime image. BuildKit tried to pull `bpf-benchmark/katran-artifacts:arm64` from Docker Hub and reported pull access denied. Debug artifact: `.state/runner-contracts/run.aws-arm64.test.7bdb6458`.
2. Failed with the same local Katran image pull issue. Debug artifact: `.state/runner-contracts/run.aws-arm64.test.3c6360ee`.
3. Failed after the first Dockerfile/build change because the kernel build still used amd64 userspace for an ARM64 kernel compile. The log included unsupported ARM64 flags such as `-mlittle-endian` and `-msign-return-address=non-leaf`. Debug artifact: `.state/runner-contracts/run.aws-arm64.test.3682d244`.
4. Failed after removing the `TARGETPLATFORM` default while still using `FROM --platform=$TARGETPLATFORM`; Docker reported an invalid empty platform. Debug artifact: `.state/runner-contracts/run.aws-arm64.test.dfceed20`.
5. Passed after removing the explicit Dockerfile platform wiring, using CLI platform selection, deleting stale ARM64 tars, rebuilding the images, and rerunning the suite. Final run token: `run.aws-arm64.test.403bdbf4`.

## Remote Flow

The final successful run:

- Built ARM64 Katran artifacts, including `xdproot`, `katran_server_grpc`, and the required `.bpf.o` files.
- Built the ARM64 runner runtime image.
- Built the ARM64 kernel and kinsn modules.
- Compiled micro BPF objects, runner test artifacts, and the Rust daemon for `aarch64-unknown-linux-gnu`.
- Uploaded and loaded `bpf-benchmark/runner-runtime:arm64` on the EC2 host.
- Installed custom kernel `7.0.0-rc2` on Amazon Linux 2023.
- Configured 2 GiB swap.
- Ran the AWS ARM64 test path successfully.
- Synced results and terminated the EC2 instance.

## Worktree Notes

No commit was made.

Pre-existing unrelated dirty files and result artifacts were left in place. The relevant code changes for this ARM64 fix are in:

- `runner/containers/katran-artifacts.Dockerfile`
- `runner/containers/runner-runtime.Dockerfile`
- `runner/mk/build.mk`
