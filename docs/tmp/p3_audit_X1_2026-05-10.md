# Phase 3 Cleanup Deep Audit X1

Date: 2026-05-10  
Repository: `/home/yunwei37/workspace/bpf-benchmark`  
Branch: `main`  
Mode: read-only source audit; source files were not modified. This report is the only written output.

## Evidence

- Required docs read: `docs/tmp/p3_review_wave5_2026-05-10.md`, `docs/bbprogram-architecture-design.md`, and CLAUDE.md sections `No ReJIT Filtering`, `No Dead Code, Fallback, or Silenced Errors`, `Unit Test Quality`.
- `wc -l` scoped total: `8708` lines.
- `cargo metadata --manifest-path bpfopt/Cargo.toml --no-deps --format-version 1`: package `bpfopt`, lib target, bin target, and integration tests under workspace root `/home/yunwei37/workspace/bpf-benchmark/bpfopt`.
- `rg -n "lower\(" ...scoped files`: no matches.
- `rg -n "live_rejit_programs|LifecycleAbort|before_rejit|_EXCLUDED|exclude|prog_type.*exclude|prog_type.*skip|skip.*program|filter.*program|ReJIT.*skip|skip.*ReJIT" ...scoped files`: only `map_info.rs:49` doc text about map types excluded from direct map-value access; no ReJIT filtering hooks.
- `git status --short --branch`: tree was already dirty on `main...origin/main [ahead 3]`; several scoped pass files were already modified before this audit.

## `bpfopt/crates/bpfopt/src/passes/dce.rs`

### (a) Cross-pass duplicate helpers

- No cross-pass duplicate helper found in this file. `would_empty_all_bodies()` at `dce.rs:58` and `is_removable_dead_def()` at `dce.rs:73` are pass-specific.

### (b) Meaningless wrappers

- No trivial wrapper found. `run_on_bbprogram()` at `dce.rs:25` contains the pass body and is called by `BpfPass::run()` at `dce.rs:20-22`.

### (c) Non-design-conforming patterns

- No `lower(prog)`.
- No production `BpfProgram::new(insns)`.
- No PC-keyed flat program loop or `program.insns[pc]`.
- No direct flat `Vec<BpfInsn>` mutation path. Mutation is via `prog.delete_insn(def)?` at `dce.rs:42`.

### (d) Fallback / legacy / silenced errors

- No `.ok()`, `unwrap_or*`, `let _ =`, `if let Err(_)`, production `unwrap()` / `expect()` / `panic!()`, fallback, or legacy comments found.

### (e) ReJIT filtering hints

- None found. This pass does not filter programs from ReJIT.

Recommendation: acceptable.

## `bpfopt/crates/bpfopt/src/passes/noop.rs`

### (a) Cross-pass duplicate helpers

- No duplicate helper found.

### (b) Meaningless wrappers

- `noop.rs:24-26` `pub fn run_on_bbprogram()` is a trivial unchanged-result wrapper and `rg` found no caller outside its own definition. `BpfPass::run()` already returns `PassResult::unchanged()` at `noop.rs:19-21`.

Recommendation: delete `run_on_bbprogram()` or add a real caller if this is intended as a test seam. Current form is stale public API.

### (c) Non-design-conforming patterns

- No `lower(prog)`.
- No production `BpfProgram::new(insns)`.
- No PC-keyed loop or direct instruction indexing.
- No `Vec<BpfInsn>` mutation path.

### (d) Fallback / legacy / silenced errors

- None found.

### (e) ReJIT filtering hints

- None found.

## `bpfopt/crates/bpfopt/src/passes/const_prop.rs`

### (a) Cross-pass duplicate helpers

- `const_prop.rs:794-856` `eval_binary_alu()` duplicates the constant ALU evaluator shape in `map_inline.rs:4963-5010` `apply_constant_alu()`. `const_prop` supports more ops (`DIV`, `MOD`, `XOR`, `ARSH`); `map_inline` uses a subset.

Recommendation: hoist a shared constant-ALU evaluator to `insn.rs` or `analysis/bbprogram_helpers.rs`; let passes opt into supported op subsets at call sites.

- `const_prop.rs:955-965` `emit_constant_load()` duplicates the scalar constant materialization logic in `map_inline.rs:4377-4389`.

Recommendation: hoist a shared `emit_scalar_const_load(dst, value, width)` helper to `insn.rs`.

- `const_prop.rs:972-983` `decode_ldimm64_site()` duplicates `map_inline.rs:4945-4952` `decode_ldimm64()` and the private combine logic in `analysis/bbprogram_helpers.rs:169-172`.

Recommendation: add `BBProgram::ldimm64_value(site)` or `bbprogram_helpers::ldimm64_value(prog, site)` and use it from both passes.

- `const_prop.rs:44-388` carries a verifier-backed register constant oracle and `const_prop.rs:540-623` carries block state solving/simulation. This overlaps conceptually with the simple register-state framework in `analysis/bbprogram_helpers.rs:104-167`, but the verifier-post-state consensus is pass-specific.

Recommendation: do not hoist the oracle wholesale. Only hoist low-level ALU/LDIMM utilities unless a broader dataflow framework emerges.

### (b) Meaningless wrappers

- `const_prop.rs:464-466` `run_on_bbprogram()` only calls private `run_on_bbprogram_inner()` at `const_prop.rs:468`.

Recommendation: inline/delete the private inner function or make the public function contain the body.

### (c) Non-design-conforming patterns

- No `lower(prog)`.
- No production `BpfProgram::new(insns)`.
- No full-program `for pc in 0..insns.len()` loop or direct `program.insns[pc]`.
- `Vec<BpfInsn>` appears only as replacement payloads (`const_prop.rs:440`, `const_prop.rs:445`, `const_prop.rs:478`, `const_prop.rs:955`, `const_prop.rs:985`); mutation is via `prog.replace_range(...)` at `const_prop.rs:511`. Acceptable.

### (d) Fallback / legacy / silenced errors

- `const_prop.rs:474` uses `prog.oracle.as_deref().unwrap_or(&[])`.

Recommendation: warning. If `const_prop` requires verifier states in production, replace this with an explicit error. If empty oracle is intentionally valid for tests/offline no-op behavior, document that invariant near the pass metadata.

- `const_prop.rs:204` uses `unwrap_or_else(|| state_pc_insertion_ordinal(...))` for verifier post-state PCs not mapped to current sites. This is a deliberate current-PC insertion policy for `InsnDeltaState`, not a discarded fallible result.

Recommendation: acceptable, but the policy belongs in a named helper comment because it is subtle.

- `const_prop.rs:237-238`, `const_prop.rs:252-253` use `unwrap_or(0)` for missing observation counters.

Recommendation: acceptable; these are local count defaults, not swallowed errors.

### (e) ReJIT filtering hints

- None found.

## `bpfopt/crates/bpfopt/src/passes/bounds_check_merge.rs`

### (a) Cross-pass duplicate helpers

- `bounds_check_merge.rs:210-213` `is_terminator_site()` duplicates `map_inline.rs:3250-3252`.

Recommendation: hoist to `BBProgram::is_terminator_site(site)` or `analysis/bbprogram_helpers.rs`.

- `bounds_check_merge.rs:228-236` `current_program_len()` duplicates the slot-length concept in `map_inline.rs:3299-3310` `program_slot_len()` and overlaps with `prefetch.rs:780-790` `block_slot_bounds()`.

Recommendation: hoist a `BBProgram::program_slot_len()` / `BBProgram::block_slot_bounds()` API.

- `bounds_check_merge.rs:17-22`, `bounds_check_merge.rs:563-622`, and `bounds_check_merge.rs:636-640` implement a packet data/data_end register tracker. This overlaps with packet-state tracking in `prefetch.rs:459-491`, `prefetch.rs:519-606`, though the state payload differs (`root_id/const_off` vs `def_end_pc`).

Recommendation: lower priority. Consider a shared packet-context transfer helper only if another cleanup pass can keep the differing state payloads readable.

### (b) Meaningless wrappers

- No pure no-op wrapper found. Small helpers like `reg_state()` at `bounds_check_merge.rs:632-634` and `clear_states()` at `bounds_check_merge.rs:636-640` are local readability helpers; acceptable.

### (c) Non-design-conforming patterns

- No `lower(prog)`.
- No production `BpfProgram::new(insns)`.
- No direct `program.insns[pc]`.
- `bounds_check_merge.rs:501-505` loops over current PC values between two guard sites and looks up sites through `pc_sites`. This is not a flat `insns[pc]` scan, but it is still PC-keyed production analysis.

Recommendation: convert this to an ordered `InsnSite` range traversal, preferably via the same frame/site range helper recommended for `map_inline`.

- `Vec<BpfInsn>` is only replacement payload in `apply_rewrites()` (`bounds_check_merge.rs:170-174`, `bounds_check_merge.rs:195`); mutation goes through `BBProgram`. Acceptable.

### (d) Fallback / legacy / silenced errors

- `bounds_check_merge.rs:118` uses `group.last().copied().unwrap_or(i)` even though `group` is initialized non-empty at `bounds_check_merge.rs:100`.

Recommendation: remove the fallback. Use the known `j`/last index directly or turn the invariant into an explicit error path.

- `bounds_check_merge.rs:584-588` synthesizes a new root id when `data_end` is loaded before `data`.

Recommendation: acceptable heuristic state modeling, not a swallowed fallible result.

- `bounds_check_merge.rs:598` and `bounds_check_merge.rs:602` use `states.get(...).unwrap_or(RegValue::Unknown)`.

Recommendation: warning. Invalid BPF registers should probably be rejected by shared register access helpers rather than silently becoming `Unknown`.

### (e) ReJIT filtering hints

- `bounds_check_merge.rs:69-71` returns unchanged for unsupported `prog_type` layout. This is pass applicability, not a ReJIT program filter: the program is still processed and ReJIT can surface naturally.
- No forbidden ReJIT filtering hook found.

## `bpfopt/crates/bpfopt/src/passes/branch_flip.rs`

### (a) Cross-pass duplicate helpers

- No scoped duplicate helper with another pass found. `BranchFlipSite::total_len()` at `branch_flip.rs:203-208` is pass-specific.

### (b) Meaningless wrappers

- `ProfiledBranchFlipPass` at `branch_flip.rs:54-57` is a real adapter that carries parsed profile data into the `BpfPass` object. Acceptable.
- `BranchFlipCliArgs` at `branch_flip.rs:89-111` is real CLI parsing state. Acceptable.

### (c) Non-design-conforming patterns

- No `lower(prog)`.
- No production `BpfProgram::new(insns)`.
- No direct `program.insns[pc]`.
- `branch_flip.rs:278-279` loops over a current PC range to detect exterior branches into a diamond interior.

Recommendation: warning. This is site-safe enough today because it checks `branch_target_pcs()`, but the BBProgram cleanup target should expose a site/slot range helper so pass logic does not manually reason over PC intervals.

- Mutation goes through `replace_terminator()` and `permute_blocks()` at `branch_flip.rs:455-477`. Acceptable.

### (d) Fallback / legacy / silenced errors

- No `.ok()`, `unwrap_or*`, `let _ =`, `if let Err(_)`, production `unwrap()` / `expect()` / `panic!()` found.
- `branch_flip.rs:232-236` bails when program-level PMU data is missing, and `branch_flip.rs:267-275` bails when candidate per-site profile data is missing. This conforms to the real-PGO rule.

### (e) ReJIT filtering hints

- `branch_flip.rs:243-251` returns `PassResult::skipped()` when real PMU miss rate exceeds the pass threshold. This skips an optimization site/pass outcome, not ReJIT of the BPF program.
- No forbidden ReJIT filtering hook found.

## `bpfopt/crates/bpfopt/src/passes/map_inline.rs`

### (a) Cross-pass duplicate helpers

- `map_inline.rs:271-304` `current_sites_after_in_frame()` / `current_sites_before_in_frame_rev()` overlaps with ordered frame/range traversal in `prefetch.rs:728-745` `sites_in_frame_pc_range()` and `prefetch.rs:748-764` `sites_in_block_pc_range()`.

Recommendation: hoist ordered frame/site traversal to `BBProgram`, with forward/reverse and optional start/end site/PC bounds.

- `map_inline.rs:3250-3252` `is_terminator_site()` duplicates `bounds_check_merge.rs:210-213`.

Recommendation: hoist to `BBProgram::is_terminator_site(site)`.

- `map_inline.rs:3299-3310` `program_slot_len()` duplicates `bounds_check_merge.rs:228-236` and overlaps `prefetch.rs:780-790`.

Recommendation: hoist `program_slot_len()` and `block_slot_bounds()` to BBProgram.

- `map_inline.rs:4377-4389` `emit_constant_load()` duplicates `const_prop.rs:955-965` in purpose.

Recommendation: hoist scalar constant load emission to `insn.rs`.

- `map_inline.rs:4945-4952` `decode_ldimm64()` duplicates `const_prop.rs:972-983`.

Recommendation: hoist `ldimm64_value(prog, site)`.

- `map_inline.rs:4963-5010` `apply_constant_alu()` overlaps with `const_prop.rs:794-856` `eval_binary_alu()`.

Recommendation: hoist the common evaluator or reuse `const_prop`'s full evaluator from a neutral helper module.

### (b) Meaningless wrappers

- `map_inline.rs:2506-2508` `run_on_bbprogram()` only calls `MapInlinePass::run_bbprogram()`, and `map_inline.rs:2511` makes `run_bbprogram()` a private associated function with no `self` use.

Recommendation: inline the body into the free function or make `BpfPass::run()` call a real free helper directly.

- `map_inline.rs:4555-4557` `insert_materialization_site()` only calls `BTreeSet::insert()` and has one call site at `map_inline.rs:1679`.

Recommendation: delete and inline.

- `map_inline.rs:4502-4504` `missing_snapshot_anyhow()` only constructs `SiteRewriteError::MissingSnapshot`.

Recommendation: inline unless the enum construction is expected to change.

- `map_inline.rs:1780-1787` `site_replacement()` validates the site by calling `site_pc()` and then constructs `SiteReplacement`. This is small, but it enforces a real invariant before rewrites.

Recommendation: acceptable.

### (c) Non-design-conforming patterns

- No `lower(prog)`.
- No production `BpfProgram::new(insns)`.
- No direct `program.insns[pc]`.
- `map_inline.rs:1191`, `map_inline.rs:1783`, `map_inline.rs:4102`, and `map_inline.rs:4377` use `Vec<BpfInsn>` as replacement payloads. Actual mutation is site-keyed through `apply_replacements_and_deletions()` at `map_inline.rs:3177-3205`, `replace_site()` at `map_inline.rs:3208-3222`, and `delete_site()` at `map_inline.rs:3224-3239`.

Recommendation: acceptable; this is not a flat program mutation path.

- PC values remain as diagnostics and boundary lookups (`site_pc()` at `map_inline.rs:248-253`, inline hint resolution at `map_inline.rs:2052-2080`). Acceptable.

### (d) Fallback / legacy / silenced errors

- `map_inline.rs:1595-1598` turns any `find_constant_stack_bytes_with_limit()` error into `None` inside `materialization_for_snapshot_key()`.

Recommendation: change return type to `Result<Option<ConstantStackBytes>, String>` and propagate BBProgram/invariant errors while still returning `Ok(None)` for true pattern mismatch.

- `map_inline.rs:1658`, `map_inline.rs:1662`, `map_inline.rs:1686`, and `map_inline.rs:1690` use fallback values after invariants that should already prove conversion/presence.

Recommendation: warning. Replace with checked collection or explicit invariant errors; avoid `usize::MAX` / zero / current-site fallbacks.

- `map_inline.rs:1706`, `map_inline.rs:1719`, `map_inline.rs:1738`, `map_inline.rs:3557`, and `map_inline.rs:3944` use diagnostic `unwrap_or(usize::MAX)`.

Recommendation: warning, not P0. Propagate site-PC lookup errors where convenient, or format the `InsnSite` if current PC is unavailable.

- `map_inline.rs:1970` uses `unwrap_or(0)` to fabricate `map_id=0` in `kernel_mutable_writer_reason()` when no map id or metadata exists.

Recommendation: warning. Emit an explicit unknown-map reason instead of a fake id.

- `map_inline.rs:3359` uses `unwrap_or(0)` for hard hint `stack_off` when no r2 stack setup is found.

Recommendation: warning. Make `ConstantKey.stack_off` optional or split hinted keys from verifier/stack-derived keys.

- `map_inline.rs:4153-4158` discards `resolve_key_pointer_origin()` errors as `None`.

Recommendation: split "not a map-value origin" from analysis errors; propagate the latter.

- `map_inline.rs:4567-4582` `find_r2_stack_pointer_setup_simple()` returns `None` on `find_prev_reg_def_within()` errors.

Recommendation: change to `Result<Option<(InsnSite, InsnSite, i16)>>` and propagate errors.

- `map_inline.rs:4663-4685`, `map_inline.rs:5253-5261`, `map_inline.rs:5274-5282`, and `map_inline.rs:5391-5403` swallow stack/constant-resolution errors as non-matches.

Recommendation: convert helper APIs to carry `Result<Option<_>>`, preserving true non-match as `Ok(None)` and surfacing BBProgram/invariant failures.

- `map_inline.rs:4259-4266`, `map_inline.rs:4273-4277`, and `map_inline.rs:3851-3853` silently no-op some direct map-value / map-in-map opportunities when snapshots are missing/skipped or maps are mutable.

Recommendation: acceptable as optimization non-applicability only if diagnostics remain sufficient. For fail-fast cleanup, prefer explicit `SkipReason`/diagnostic over silent `Ok(None)` when the pass found a candidate but cannot use required side input.

### (e) ReJIT filtering hints

- No forbidden ReJIT filtering hook found. All `record_skip()` / `PassResult::skipped` uses are pass-site outcomes, not program exclusion from ReJIT.

## `bpfopt/crates/bpfopt/src/passes/map_inline/map_info.rs`

### (a) Cross-pass duplicate helpers

- `map_info.rs:142-167` `analyze_map_info()` duplicates map metadata projection also present in the test-only `SnapshotMapProvider` implementation in `pass.rs:429-448`.

Recommendation: when the test-only flat facade is removed, keep one production map metadata conversion helper near `PassContext` or `map_info`.

### (b) Meaningless wrappers

- `map_info.rs:65-67` `supports_direct_value_inline()` is a pure alias for `supports_direct_value_access()`.

Recommendation: delete and use `supports_direct_value_access()` directly, or give the inline-specific predicate distinct logic.

- `map_info.rs:119-139` `MapInfoAnalysis` has only a `#[cfg(test)]` `run()` method. In production it is an exported empty struct via `map_inline.rs:18` / `passes/mod.rs:33`.

Recommendation: migrate tests to `BBProgram` + `analyze_map_info()` and delete this facade, or gate/export it only under `#[cfg(test)]`.

- `map_info.rs:169-208` `analyze_map_info_from_bpf_program()`, `collect_map_references()`, and `collect_map_references_with_bindings()` are test-only flat-program adapters that recreate `BpfProgram`.

Recommendation: delete after map-info tests are BBProgram-native.

### (c) Non-design-conforming patterns

- No production `lower(prog)`.
- `map_info.rs:204`, `map_info.rs:496`, `map_info.rs:540`, `map_info.rs:554`, and `map_info.rs:576` use `BpfProgram::new(...)`; all are under `#[cfg(test)]` helper/test code.

Recommendation: acceptable for production gates, but stale BBProgram migration scaffolding.

- `map_info.rs:246-278` `collect_map_bindings()` is `#[cfg(test)]` and uses a flat `while pc < insns.len()` plus `insns[pc]` at `map_info.rs:256-257`.

Recommendation: acceptable under the production-only design gate, but delete when tests migrate.

- `map_info.rs:557` mutates `program.insns` in a test.

Recommendation: acceptable only as test scaffolding; migrate test setup to BBProgram/map binding APIs.

### (d) Fallback / legacy / silenced errors

- Production code has no `.ok()`, `unwrap_or*`, `let _ =`, `if let Err(_)`, or panic-family use.
- `map_info.rs:287-302` `resolve_map_ref()` returns `(None, None)` for unresolved FD-style refs. Production then records map info unavailable at `map_inline.rs:2691-2694`.

Recommendation: warning. If v3 canonical map bindings are mandatory, unresolved production map refs should be an error. If unresolved FD refs are a valid no-op, document that explicitly.

- Panic-family hits are test-only `expect()` calls at `map_info.rs:434`, `459`, `471`, `484`, `497`, `565`, and `581`; acceptable for tests.

### (e) ReJIT filtering hints

- `map_info.rs:49` says per-CPU hash maps are deliberately excluded from direct value access. This is map-value inline applicability, not a BPF program exclusion from ReJIT.
- No forbidden ReJIT filtering hook found.

## Aggregate Cleanup Task List

1. Hoist BBProgram site/slot helpers: `is_terminator_site`, `program_slot_len`, `block_slot_bounds`, ordered frame forward/reverse site traversal. Primary lines: `bounds_check_merge.rs:210`, `bounds_check_merge.rs:228`, `map_inline.rs:271`, `map_inline.rs:287`, `map_inline.rs:3250`, `map_inline.rs:3299`, `prefetch.rs:728`, `prefetch.rs:780`.

2. Fix fail-fast issues in `map_inline` Option-returning analysis helpers. Start with `materialization_for_snapshot_key()` (`map_inline.rs:1595-1598`), `find_r2_stack_pointer_setup_simple()` (`map_inline.rs:4567-4582`), direct map-value origin handling (`map_inline.rs:4153-4158`), stack slot resolution (`map_inline.rs:5253-5282`), and alias adjustment (`map_inline.rs:5391-5403`).

3. Remove diagnostic/invariant fallbacks in `map_inline`: `usize::MAX` PC fallbacks (`map_inline.rs:1706`, `1719`, `1738`, `3557`, `3944`), fake `map_id=0` (`map_inline.rs:1970`), fake hinted `stack_off=0` (`map_inline.rs:3359`), and invariant defaults in constant stack byte collection (`map_inline.rs:1658`, `1662`, `1686`, `1690`).

4. Hoist shared constant utilities: const ALU evaluator, scalar constant load emission, and LDIMM64 decode. Primary lines: `const_prop.rs:794`, `const_prop.rs:955`, `const_prop.rs:972`, `map_inline.rs:4377`, `map_inline.rs:4945`, `map_inline.rs:4963`.

5. Delete trivial/stale wrappers: `noop.rs:24-26`, `const_prop.rs:464-466`, `map_inline.rs:2506-2511`, `map_inline.rs:4555-4557`, `map_inline.rs:4502-4504`, `map_info.rs:65-67`.

6. Migrate and delete the remaining test-only flat map-info facade: `map_info.rs:135-208`, `map_info.rs:246-278`, and `BpfProgram::new` test uses at `map_info.rs:496`, `540`, `554`, `576`. This also enables removing the empty production `MapInfoAnalysis` export.

7. Replace PC-keyed production loops with site/slot-range APIs: `bounds_check_merge.rs:501-505` and `branch_flip.rs:278-279`.

8. Decide explicit policy for missing verifier oracle in `const_prop` (`const_prop.rs:474`) and unresolved production map refs in `map_info.rs:287-302`.

## Estimated LOC Delta

Rough net delta if all recommendations are applied:

- Shared BBProgram/site helpers: `-35` to `-60` net scoped LOC after adding shared API.
- Shared constant utilities: `-45` to `-80` net scoped LOC.
- Trivial wrapper deletion: `-20` to `-35` scoped LOC.
- Map-info flat test facade migration/deletion: `-80` to `-130` scoped LOC, plus possible test changes outside scope.
- Fail-fast conversions in `map_inline`: likely `+20` to `+60` LOC because `Result<Option<_>>` plumbing is more explicit.
- PC-keyed loop replacement: approximately neutral to `-15` LOC once shared traversal helpers exist.

Estimated scoped net: **about `-160` to `-260` LOC**. Repository-wide net could be larger if the test-only `BpfProgram` facade in `pass.rs` is removed in the same cleanup.
