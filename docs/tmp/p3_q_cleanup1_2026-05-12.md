# P3 Q Cleanup Items 1-4

Date: 2026-05-12
Branch: main

## Scope Notes

- Executed Q-1 through Q-4 in the requested structural-cleanup area.
- R's concurrent wrapper/test cleanup changed the test surface while this ran. The current test suite is 204 tests, not the original 216+ count.
- LOC accounting below uses the final diff for the requested affected files against `HEAD`: 1,149 insertions / 2,809 deletions, net **-1,660 LOC**. The unstaged working-tree delta after composing with R's concurrent edits is 331 insertions / 743 deletions, net **-412 LOC**.

## Q-1: Dead Production Markers And Generic Report Wrappers

Action taken:
- Removed `PolicyConfig` and the `PassContext.policy` field; `PassContext::try_baseline()` no longer constructs dead policy state (`bpfopt/crates/bpfopt/src/pass.rs:401`, `bpfopt/crates/bpfopt/src/pass.rs:744`).
- Removed generic report action wrappers (`PassAction`, `PassReportSite`, `PassReportPc`, `finalize_pass_reports`) and replaced them with direct site-to-PC report materialization through `report_site_pc()` (`bpfopt/crates/bpfopt/src/pass.rs:666`, `bpfopt/crates/bpfopt/src/main.rs:1802`, `bpfopt/crates/bpfopt/src/main.rs:1817`).
- Verified `MapInfoAnalysis` marker is absent; renamed the stale result alias so no `MapInfoAnalysis` symbol remains.

LOC delta: approximately **-95 LOC**.

## Q-2: Hint Validation/Formatting And Hex Helper Collapse

Action taken:
- Centralized inline-hint mode validation in lift and re-exported it for CLI parsing (`bpfopt/crates/bpfopt/src/analysis/bbprogram_lift.rs:165`, `bpfopt/crates/bpfopt/src/analysis/mod.rs:17`, `bpfopt/crates/bpfopt/src/main.rs:828`).
- Removed main's duplicate anchor-mode validator and formatter.
- Promoted `pass::hex_bytes()` and reused it for CLI inlined-map reports (`bpfopt/crates/bpfopt/src/pass.rs:299`, `bpfopt/crates/bpfopt/src/main.rs:1833`).

LOC delta: approximately **-70 LOC**.

## Q-3: `MapMetadata` / `MapInfo` Collapse

Action taken:
- Kept `MapMetadata` as the single map metadata struct and added the `MapInfo` behavior to it (`bpfopt/crates/bpfopt/src/pass.rs:232`, `bpfopt/crates/bpfopt/src/passes/map_inline/map_info.rs:17`, `bpfopt/crates/bpfopt/src/passes/map_inline/map_info.rs:19`).
- `map_info` now returns cloned `MapMetadata` directly from side input instead of rebuilding a duplicate no-name struct (`bpfopt/crates/bpfopt/src/passes/map_inline/map_info.rs:102`).
- Deleted the `snapshot_map_info` conversion path; direct snapshot/pseudo-map-value paths now consume metadata from side input.
- Updated the allowed `map_info_tests.rs` fixtures for the shared `name` field.

LOC delta: approximately **-135 LOC**.

## Q-4: Remove `BBProgram` Ownership Of `MapInlineSideInput`

Action taken:
- Removed `BBProgram`'s `MapInlineSideInput` import, field, getter, and attach method; the IR now only retains generic map bindings and BTF/profile side inputs (`bpfopt/crates/bpfopt/src/analysis/bbprogram.rs:81`, `bpfopt/crates/bpfopt/src/analysis/bbprogram.rs:790`).
- Removed lift-time map-inline attachment; lift now attaches only generic side inputs and profile annotations (`bpfopt/crates/bpfopt/src/analysis/bbprogram_lift.rs:150`, `bpfopt/crates/bpfopt/src/analysis/bbprogram_lift.rs:160`).
- `map_inline` builds a borrowed `MapInlineSideInput` from `PassContext` at pass runtime and passes it through map-inline helpers (`bpfopt/crates/bpfopt/src/passes/map_inline.rs:35`, `bpfopt/crates/bpfopt/src/passes/map_inline.rs:60`, `bpfopt/crates/bpfopt/src/passes/map_inline.rs:1153`, `bpfopt/crates/bpfopt/src/passes/map_inline.rs:1201`).

LOC delta: approximately **-112 LOC**.

## Total LOC Delta

- Final affected-file diff vs `HEAD`: **-1,660 LOC** across the requested cleanup files.
- Current unstaged composed delta after R's concurrent edits: **-412 LOC**.
- Acceptance threshold met: **>= 400 LOC net reduction**.

## Gate Results

- `cd bpfopt && cargo build --release`: **pass**
- `cd bpfopt && cargo test --release`: **pass**, 204 tests passed
- `cd bpfopt && cargo clippy --release --all-targets -- -D dead_code -D warnings`: **pass**
- `cd bpfopt && cargo fmt --all && cargo fmt --all --check`: **pass**, 0 diff

## Skipped Items

None. The only acceptance caveat is the test count: current suite size is 204 because concurrent R cleanup removed interface-only tests before the final gate run.
