# P1-H Fix Report - 2026-05-10

## Scope

- Fixed the 6 P1 + 3 P2 issues from `docs/tmp/p1g_review_2026-05-10.md`.
- Skipped the DCE regression as requested.
- Touched only `bpfopt/crates/bpfopt/src` plus this report.
- Did not commit.

Pre-existing unrelated dirty/untracked paths remain in the worktree (`runner/config/passes/noop/default.yaml`, `corpus/results/...`, `docs/paper`, P1-F/P1-G temp docs). This task did not edit them.

## Item Fixes

1. `bounds_check_merge` RewritePlan + BTF remap
   - `bpfopt/crates/bpfopt/src/passes/bounds_check_merge.rs:178-199`
   - Changed the pass to build the initial `RewritePlan`, compose cleanup addr maps with `compose_addr_maps`, and perform a single `commit_rewrite_output(..., BtfRemapPolicy::Remap)`.
   - Removed the direct `program.insns = cleaned_insns` cleanup path from this pass.
   - Added `bpfopt/crates/bpfopt/src/passes/bounds_check_merge_tests.rs:399` to verify func_info/line_info offsets remap across the merge.

2. `branch_flip` `.ok()` removal
   - `bpfopt/crates/bpfopt/src/passes/branch_flip.rs:479`
   - `bpfopt/crates/bpfopt/src/passes/branch_flip.rs:512`
   - Replaced conversion `.ok()` drops with invariant `expect(...)` on values already proven positive/in-range by BPF branch encoding.

3. `wide_mem` sentinel removal
   - `bpfopt/crates/bpfopt/src/passes/wide_mem.rs:47`
   - `bpfopt/crates/bpfopt/src/passes/wide_mem.rs:519`, `:554`, `:590`, `:624`
   - Added required binding lookup and explicit integer conversion errors.
   - Replaced all `unwrap_or(-1)` sentinel paths and binding fallbacks in checked pass logic.

4. `map_inline` use/def unification
   - `bpfopt/crates/bpfopt/src/passes/map_inline.rs:13`
   - `bpfopt/crates/bpfopt/src/passes/map_inline.rs:4156`, `:4291`, `:4356`, `:4536`, `:5243`, `:5342`, `:5350`
   - Removed local `insn_uses_reg` / `insn_defines_reg` helpers.
   - All map_inline single-instruction use/def checks now call shared `analysis::insn_use_def_set`.

5. `MapRefsAnalysis` production use
   - `bpfopt/crates/bpfopt/src/passes/map_inline/map_info.rs:118`
   - `bpfopt/crates/bpfopt/src/passes/map_inline/map_info.rs:161`
   - `MapInfoAnalysis` now consumes `MapRefsAnalysis::run(program)`.
   - Test-only reference collection also goes through `MapRefsAnalysis` with explicit program bindings.

6. Pseudo-map API consolidation
   - `bpfopt/crates/bpfopt/src/insn.rs:424`, `:429`
   - Added `BpfInsn::is_map_pseudo()` and `BpfInsn::map_pseudo_kind()`.
   - `bpfopt/crates/bpfopt/src/analysis/map_refs.rs:43-46` now uses those APIs.
   - Updated map-info/main tests to use `MapPseudo::* .src_reg()` rather than direct pseudo constants where they were in non-`*_tests.rs` files.

7. `branch_flip` oversized-delta pass regression
   - `bpfopt/crates/bpfopt/src/passes/branch_flip_tests.rs:67`
   - Added a real `BranchFlipPass::run` regression whose original JA is valid at `i16::MAX`, but the flipped JCC would require `32768`, proving the pass fails fast on the actual transform path.

8. CFG subprog strong fixture
   - `bpfopt/crates/bpfopt/src/analysis/cfg_tests.rs:28-43`
   - Changed the pseudo-func fixture to target a real callback body at pc 4 with two instructions plus exit.
   - Assertions now verify pcs 4 and 5 map to callback range `4..7`.

9. LOC delta correction
   - Current P1-H source delta: `11 files changed, 238 insertions(+), 178 deletions(-)`, net `+60`.
   - P1-G review baseline reported actual source delta as net `-172`; P1-G + P1-H cumulative source delta is therefore net `-112`.

## Grep Evidence

Commands run from repo root:

```sh
rg -n "\\.ok\\(\\)|unwrap_or\\(-1\\)|#\\[allow\\(dead_code\\)\\]|unwrap_or_default|let _ = " bpfopt/crates/bpfopt/src --type rust
```

Result: no matches.

```sh
rg -n "insn_uses_reg|insn_defines_reg" bpfopt/crates/bpfopt/src/passes/map_inline.rs
```

Result: no matches.

```sh
rg -n "BPF_PSEUDO_MAP_FD|BPF_PSEUDO_MAP_IDX|BPF_PSEUDO_MAP_IDX_VALUE" \
  bpfopt/crates/bpfopt/src --type rust \
  --glob '!**/*_tests.rs' --glob '!**/tests.rs' \
  --glob '!insn.rs' --glob '!analysis/map_refs.rs'
```

Result: no matches.

```sh
rg -n "program\\.insns\\s*=|remap_btf_metadata|program\\.remap_annotations" \
  bpfopt/crates/bpfopt/src/passes/bounds_check_merge.rs \
  bpfopt/crates/bpfopt/src/passes/branch_flip.rs \
  bpfopt/crates/bpfopt/src/passes/map_inline.rs \
  bpfopt/crates/bpfopt/src/passes/dce.rs --type rust
```

Result: no matches. The remaining broad `program.insns =` hit is an inline unit-test fixture in `map_inline/map_info.rs`, not a pass commit path.

```sh
rg -n "MapRefsAnalysis" bpfopt/crates/bpfopt/src --type rust
```

Production hit: `bpfopt/crates/bpfopt/src/passes/map_inline/map_info.rs:118`.

## Verification

Final commands run after rustfmt:

```sh
cd /home/yunwei37/workspace/bpf-benchmark/bpfopt && cargo build --workspace --locked
cd /home/yunwei37/workspace/bpf-benchmark/bpfopt && cargo test --workspace --locked
cd /home/yunwei37/workspace/bpf-benchmark/daemon && cargo build --workspace --locked
cd /home/yunwei37/workspace/bpf-benchmark/daemon && cargo test --workspace --locked
```

Results:

- bpfopt build: pass
- bpfopt tests: 347 lib + 16 main + 12 CLI = 375 pass
- daemon build: pass
- daemon tests: 25 pass
- Total reported tests: 400 pass

Also run:

```sh
git diff --check -- bpfopt/crates/bpfopt/src docs/tmp/p1h_fix_2026-05-10.md
```

Result: no whitespace errors.
