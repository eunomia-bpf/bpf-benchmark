# Phase 3 Wave-5 Fix Report: map_inline B -> A

Date: 2026-05-10

## Scope Result

map_inline rating before: B

map_inline rating after: A

The wave-5 blockers from `docs/tmp/p3_review_wave4_2026-05-10.md` are resolved: map_inline planning is site-first, PC values are derived only at verifier/hint/diagnostic boundaries, the local view bridge is gone, and the required negative searches are clean.

## Per-Task Summary

1. Convert core records to site-first
   - Converted `MapLookupSite`, `MapInMapChain`, `ConstantKey`, `FixedLoadUse`, `R0UseClassification`, and `SoftNullHandler` to carry `InsnSite` identities.
   - Kept PC derivation as boundary/diagnostic lookup through `pc()`/`site_pc()`.
   - Removed production PC identity fields such as lookup call/map-load/inner-call/materialization/alias/null-check PC storage.

2. Replace PC cursor scans with site iterators
   - Reworked map-in-map chain discovery, direct map-load backtracking, R0-use classification, key materialization lookup, and null-check windows around `InsnSite` traversal.
   - Removed the flattened instruction substrate from map_inline planning.

3. Resolve PC hints at the pass boundary
   - Added boundary-resolved hint anchors so `Pc(...)` hints are converted to `InsnSite` before planning.
   - Deleted the PC-keyed production hint lookup map.

4. Delete bridge helpers
   - Deleted `MapInlineView`.
   - Deleted `sites_for_pc_iter()`.
   - Deleted `attach_constant_key_sites()`.
   - Deleted map_inline-local `current_block_start_pcs()`, `current_subprog_bounds()`, and `block_slot_len()`.

5. Use BBProgram methods
   - map_inline now uses `BBProgram::current_site_pcs()`, `BBProgram::current_pc_sites()` through the new helper, block/frame APIs, and direct site instruction access.
   - Added one traversal helper: `BBProgram::current_sites() -> anyhow::Result<Vec<InsnSite>>`.

6. Dedup `read_json_file`
   - Removed map_inline's local `read_json_file`.
   - map_inline now imports the canonical `analysis::bbprogram_helpers::read_json_file`.
   - `main.rs` was left unchanged as requested.

## LOC Delta

Measured with `wc -l` from wave-5 start to final state:

- `bpfopt/crates/bpfopt/src/passes/map_inline.rs`: 5710 -> 5572 (-138)
- `bpfopt/crates/bpfopt/src/passes/map_inline/map_info.rs`: 690 -> 712 (+22)
- `bpfopt/crates/bpfopt/src/analysis/bbprogram.rs`: 1135 -> 1139 (+4)
- Net scoped line-count delta: -112

Final `git diff --stat` for the three touched implementation files:

```text
3 files changed, 1305 insertions(+), 1241 deletions(-)
```

## Negative Search Gate

No matches in `map_inline.rs` / `map_inline/map_info.rs` for:

- `MapInlineView`
- `program\.insns\[pc\]`
- `HashMap<usize,`
- `sites_for_pc_iter`
- `attach_constant_key_sites`
- `call_pc:`
- `map_load_pc:`
- `inner_call_pc:`
- `materialization_pcs`
- `alias_copy_pcs`
- `r2_.*_pc`
- `null_check_pc`

## Verification

All requested gates passed:

```text
cargo build --release --manifest-path bpfopt/Cargo.toml
cargo test --release --manifest-path bpfopt/Cargo.toml
cargo clippy --release --manifest-path bpfopt/Cargo.toml --all-targets -- -D dead_code -D warnings
target/release/bpfopt list-passes
target/release/bpfopt --pass dce --input bpfopt/testbin/otelcol-ebpf-profiler_profiling/47_perf_unwind_php/canonicalize_output.bin --output /tmp/dce_smoke.bin --report /tmp/dce_smoke.json --prog-type tracepoint --target bpfopt/testbin/otelcol-ebpf-profiler_profiling/47_perf_unwind_php/target.json
```

Final test count: 217 passed.

`target/release/bpfopt list-passes` count: 15.

DCE smoke exit code: 0.
