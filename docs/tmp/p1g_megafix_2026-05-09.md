# P1-G Megafix Report - 2026-05-09

## Scope

- Touched code under `bpfopt/crates/bpfopt/src` only.
- Did not touch `vendor/`, `runner/`, `corpus/`, `e2e/`, `micro/`, or `daemon/src`.
- Did not run `make corpus`, e2e, or benchmark targets.
- Did not commit.

## Step Log

LOC numbers are cumulative `git diff --shortstat HEAD -- bpfopt/crates/bpfopt/src` checkpoints captured at the end of each step.

| step | work | LOC checkpoint |
|---|---|---|
| 1 | Promoted `passes/rewrite.rs` into top-level `rewrite/plan.rs` plus `rewrite/mod.rs`; updated imports and exports. | 18 files, +86/-188 |
| 2 | Removed dead pass/analysis infra: `required_analyses`, analysis registry, `Analysis::name`, `PassResult.pass_name`, unused test-only cache APIs. | 33 files, +154/-585 |
| 3 | Simplified analysis API to `Analysis::run(program)` and `cache.get::<A>(program)`. | 35 files, +205/-661 |
| 4 | Changed `AnalysisCache` to store `Arc<dyn Any + Send + Sync>` and return `Arc<A::Result>`, eliminating cache-hit Vec clones. | 35 files, +220/-673 |
| 5 | Added `analysis/site_scan.rs`, `analysis/map_refs.rs`, `MapPseudo`/packet constants in `insn.rs`, and CFG subprog range output with tests. | 38 files, +339/-675 |
| 6 | Fixed JA32/i16 branch offset handling across branch target analysis, CFG, liveness, rewrite fixup, DCE, map_inline, and branch_flip. | 38 files, +670/-855 |
| 7 | Routed DCE/map_inline deletion/replacement commit paths through `RewritePlan::build`; added shared `commit_rewrite_output` for manual permutation output. | 38 files, +737/-976 |
| 8 | Added `RegUseDefSet`/`use_def_at` in liveness; moved ccmp/cond_select/bulk_memory use/def checks to shared liveness helpers. | 38 files, +799/-1044 |
| 9 | Added `PASS_REGISTRY.required_kops`, main/PassManager gates, shared sidecar payload primitives, map pseudo helpers, packet constants, subprog helper export, and remaining duplicate cleanup. | 43 files, +1278/-1620 |

Final source delta: `43 files changed, 1278 insertions(+), 1620 deletions(-)`, net -342 LOC.

## Duplicate Cleanup Evidence

1. Site scan pattern:
   - Before: P1-E listed 12 pass-local scan loops.
   - After: one shared `analysis/site_scan.rs::iter_sites`.
   - Converted users include rotate, cond_select, extract, endian, ccmp, branch_flip, prefetch map/packet scans, skb_load_bytes, bounds_check_merge, map_inline lookup scan, and map_inline map_info scan.
   - Residual `while pc < insns.len()` grep is limited to dynamic-advance/state cleanup code: `bulk_memory` run segmentation, DCE cleanup/deletion passes, `map_inline` alias traversal, and analysis internals.

2. Branch target/fixup:
   - Before: P1-E listed branch target/fixup logic in analysis, rewrite, dce, branch_flip, map_inline, ccmp, bounds.
   - After: branch target decoding is in `BpfInsn::{branch_target_offset, branch_target_pc}` and branch rewrite is in `rewrite::fixup_all_branches`.
   - Grep for pass-local `patch_replacement_branch_targets` and `replacement_new_pcs`: 0.
   - Branch_flip still constructs its permutation map, but fixup and commit/remap are shared through `rewrite`.

3. Register use/def:
   - Before: P1-E listed local use/def/writes helpers in several passes.
   - After: `analysis/liveness.rs` owns `RegUseDefSet` and `insn_use_def_set`.
   - ccmp, cond_select, and bulk_memory no longer define local `writes_reg`/ad-hoc use-def helpers.

4. Map pseudo decode:
   - Before: P1-E listed split pseudo-map matching in pass framework, map_info, and map_inline.
   - After: `insn.rs::MapPseudo` and `BpfInsn::map_pseudo()` are the single-instruction API; `analysis/map_refs.rs` is the program-level binding extractor.
   - Production direct `src_reg() == BPF_PSEUDO_MAP_*` matching was removed from map_inline/pass/main paths. Remaining constants are test construction or the central ABI definitions.

5. KOperation availability:
   - Before: individual kop passes queried `ctx.kop_registry.is_target_available`.
   - After: `passes/mod.rs::PassMetadata.required_kops` plus main/PassManager dispatch gates.
   - Grep in kop pass files for `is_target_available`: 0.

6. Subprog bounds:
   - Before: map_inline had its own `subprog_bounds`.
   - After: `analysis/cfg.rs::{subprog_ranges, subprog_bounds}` and `CFGResult::subprog_bounds` own subprog ranges; map_inline imports the shared helper.

7. Commit chain:
   - Before: DCE/map_inline/branch_flip had pass-local `program.insns = ...`, BTF remap, and annotation remap tails.
   - After: only `rewrite/plan.rs::commit_rewrite_output` mutates `program.insns` and remaps metadata/annotations.
   - Grep evidence for `program.insns =`, `remap_btf_metadata`, `program.remap_annotations` in dce/map_inline/branch_flip: 0 outside `rewrite/plan.rs`.

8. Sidecar payload:
   - Before: kop passes packed payloads with raw shifts/ors.
   - After: `BpfInsn::{pack_u4, pack_u8, pack_u16, pack_u32, unpack_*}` are used by rotate, cond_select, ccmp, extract, endian, bulk_memory, prefetch, and pass proof decode helpers.

9. Packet ctx constants:
   - Before: `PACKET_DATA_OFFSET`/`SKB_DATA_OFF` style constants were duplicated in packet passes.
   - After: `insn.rs` owns `XDP_PACKET_DATA_OFFSET`, `XDP_PACKET_DATA_END_OFFSET`, `SKB_PACKET_DATA_OFFSET`, and `SKB_PACKET_DATA_END_OFFSET`; production packet passes consume those names.

## Defect Fixes

Defect 1 - JA32/i16 branch target and truncation:
- Added `BranchOff::{Off16, Imm32, None}` and BPF instruction helpers for target decode and checked rewrite.
- `analysis/branch_target.rs`, `analysis/cfg.rs`, liveness CFG successor handling, DCE, map_inline, and rewrite fixup now use those helpers.
- `rewrite::fixup_all_branches` now returns `Result<()>` and fails if an off16 branch cannot fit i16 or a JA32 imm cannot fit i32.

Defect 2 - branch_flip offset handling:
- branch_flip uses checked branch delta setters and the shared fixup path.
- Oversized branch deltas fail instead of truncating.

Regression tests added or updated for the bug class:
- `rewrite::plan::tests::fixup_all_branches_rewrites_ja32_imm_after_growth`
- `rewrite::plan::tests::fixup_all_branches_rejects_i16_overflow`
- `rewrite::plan::tests::fixup_all_branches_rejects_ja32_i32_overflow`
- `branch_flip_tests::test_branch_flip_rejects_oversized_manual_branch_delta`
- `branch_target_tests::branch_target_ja32_uses_imm_target`
- `cfg_tests::cfg_edges_use_ja32_imm_target`

Additional coverage added for new shared APIs:
- `analysis::site_scan_tests::*`
- `analysis::map_refs_tests::*`
- CFG subprog boundary tests.

## Byte-Identical Argument

- Step 1 was a physical move of rewrite infrastructure plus import rewrites.
- Replacement emitters still build the same `Vec<BpfInsn>` sequences; shared `RewritePlan` and `commit_rewrite_output` only centralize address-map, branch fixup, BTF remap, annotation remap, and result construction.
- Payload helpers encode the same bit positions previously encoded by raw shifts. Existing per-pass payload roundtrip/proof tests stayed green.
- Intended behavior changes are limited to fail-fast correctness for impossible branch offsets and JA32 target accounting.

## Verification

Latest verification commands:

```sh
cd /home/yunwei37/workspace/bpf-benchmark/bpfopt && cargo build --workspace --locked
cd /home/yunwei37/workspace/bpf-benchmark/bpfopt && cargo test --workspace --locked
cd /home/yunwei37/workspace/bpf-benchmark/daemon && cargo build --workspace --locked
cd /home/yunwei37/workspace/bpf-benchmark/daemon && cargo test --workspace --locked
```

Latest test counts:
- bpfopt lib: 345 passed
- bpfopt main: 16 passed
- bpfopt CLI integration: 12 passed
- daemon: 25 passed
- Total: 398 passed

Termination checks:
- Four verification commands: pass.
- Tests: 398 >= 388 + 5.
- Source LOC: deletions 1620 > insertions 1278.
- No commit made.
