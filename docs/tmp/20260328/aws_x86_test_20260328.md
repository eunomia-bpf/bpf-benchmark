# AWS x86 test report (2026-03-28)

## Status

Current status: **not fully complete**.

- `make daemon` succeeded.
- `make runner` succeeded.
- `runner/scripts/aws_x86.sh` was updated to:
  - separate `EXIT` / `INT` / `TERM` cleanup handlers
  - recover instance id from `.cache/aws-x86/state/instance.env` during cleanup
  - remove any dependency on local VM lock logic
  - force x86 AWS kernel config toward AL2023/EC2 requirements (`NET_VENDOR_AMAZON`, `ENA_ETHERNET`, `BLK_DEV_NVME`, `XFS_FS`)
  - disable `DEBUG_INFO_BTF_MODULES` for the x86 AWS build path
  - avoid `build_kernel_artifacts` command substitution so failures happen in the main shell
- I confirmed the AWS x86 path is not coupled to local VM locks:
  - `Makefile` `aws-x86-full` still directly dispatches to `runner/scripts/aws_x86.sh full`
  - `runner/scripts/aws_x86.sh` has no `with_vm_lock.py` / `vm-corpus` / `vm-e2e` check
  - `runner/scripts/x86_remote_benchmark.py` has no local VM lock usage
- I ran multiple full-lifecycle attempts.
- None of the attempts reached remote benchmark execution, so there is **no performance data to report**.
- Each failed attempt left a running EC2 instance; I terminated each one manually and verified final AWS state is clean.

Bottom line:

- Issue 2 is confirmed: remote AWS x86 is independent of local VM lock logic.
- Issue 1 is **not yet validated as fixed in practice**. Despite the script changes, the failed runs in this session still left residual instances that required manual `make aws-x86-terminate`.
- The main blocker for a true end-to-end `launch -> setup -> benchmark -> terminate` run is now the local x86 kernel/module build path, not AWS credentials or remote host setup.

## AWS environment used

```bash
AWS_X86_PROFILE=codex-ec2
AWS_REGION=us-east-1
AWS_X86_KEY_NAME=codex-arm64-test-20260319121631
AWS_X86_KEY_PATH=/home/yunwei37/.ssh/codex-arm64-test-20260319121631.pem
AWS_X86_SECURITY_GROUP_ID=sg-0ebe13c1e4c0defc9
AWS_X86_SUBNET_ID=subnet-0dfd531179110d808
AWS_X86_INSTANCE_TYPE=t3.micro
```

Verified before the reruns:

- `aws --profile codex-ec2 --region us-east-1 sts get-caller-identity` succeeded
- key path existed
- subnet `subnet-0dfd531179110d808` was `available`
- security group `sg-0ebe13c1e4c0defc9` existed in the same VPC

## Local commands run

### Build refresh

`make daemon`

- exit: `0`
- full log: `docs/tmp/20260328/logs/make_daemon_20260328_rerun.log`

`make runner`

- exit: `0`
- full log: `docs/tmp/20260328/logs/make_runner_20260328_rerun.log`

### AWS full runs

Attempt 1:

- full log: `docs/tmp/20260328/logs/make_aws_x86_full_20260328_rerun.log`
- launched instance: `i-0e2f443638d5b02f8`
- public IP: `3.90.184.186`
- shell exit: `130` after manual `SIGINT`

Attempt 2:

- full log: `docs/tmp/20260328/logs/make_aws_x86_full_20260328_final.log`
- launched instance: `i-072609e1e575fc9fd`
- public IP: `23.22.215.162`
- wrapper log exit: `2`

Attempt 3:

- full log: `docs/tmp/20260328/logs/make_aws_x86_full_20260328_final_retry.log`
- launched instance: `i-0e9ba1b33a3eed068`
- public IP: `34.235.116.82`
- wrapper log exit: `2`
- inner `make` failure reported `Error 143`

## What happened in each lifecycle attempt

### Attempt 1: reached remote reboot, then custom-kernel SSH never recovered

Observed path:

- local bundle preparation succeeded
- EC2 launch succeeded
- stock-kernel SSH succeeded
- remote prereq install succeeded
- remote kernel install reached `dracut`
- remote console later showed reboot into `Booting 'Codex x86 (7.0.0-rc2+)'`
- after reboot, `wait_for_ssh` never recovered SSH on the custom kernel

Important evidence:

- raw log showed:

```text
[aws-x86] Waiting for SSH on 3.90.184.186
[aws-x86] Reusing cached x86 kernel artifacts for 7.0.0-rc2+
[aws-x86] Installing kernel 7.0.0-rc2+ on 3.90.184.186
dracut-install: Failed to find module 'xfs'
dracut[E]: FAILED:  /usr/lib/dracut/dracut-install -D /var/tmp/dracut.AznQsg/initramfs --kerneldir /lib/modules/7.0.0-rc2+/ -m xfs ext4
```

- EC2 console output showed the guest later rebooted and selected:

```text
Booting `Codex x86 (7.0.0-rc2+)'
```

- local cached modules artifact from that run was incomplete; it did not contain the expected AWS-relevant module set

Cleanup result:

- I interrupted the stuck run with `SIGINT`
- the instance remained `running`
- I terminated it manually
- final AWS state:

```text
i-0e2f443638d5b02f8  terminated  User initiated (2026-03-29 01:27:39 GMT)
```

### Attempt 2: local x86 modules build failed before upload

Changes before this attempt:

- forced x86 AWS config toward EC2 requirements
- switched `ENA_ETHERNET`, `BLK_DEV_NVME`, `XFS_FS` to `=y`
- disabled `DEBUG_INFO_BTF_MODULES`
- removed command substitution around `build_kernel_artifacts`

Observed path:

- local x86 kernel image build completed
- EC2 launch succeeded
- stock-kernel SSH succeeded
- during `build_kernel_artifacts`, local `make -C vendor/linux-framework -j1 modules` failed

Raw failure:

```text
Failed to parse base BTF './vmlinux': -5
make[3]: *** [scripts/Makefile.modfinal:60: drivers/base/regmap/regmap-w1.ko] Error 1
make[2]: *** [/home/yunwei37/workspace/bpf-benchmark/vendor/linux-framework/Makefile:2032: modules] Error 2
[aws-x86][ERROR] kernel builtin module missing from /home/yunwei37/workspace/bpf-benchmark/vendor/linux-framework/modules.builtin: kernel/drivers/net/ethernet/amazon/ena/ena.ko
```

Cleanup result:

- the run exited
- the instance still remained `running`
- I terminated it manually
- final AWS state:

```text
i-072609e1e575fc9fd  terminated  User initiated (2026-03-29 02:05:50 GMT)
```

### Attempt 3: local kbuild got externally terminated during syncconfig

Observed path:

- local x86 kernel image build completed
- EC2 launch succeeded
- stock-kernel SSH succeeded
- while re-entering the x86 local build path, kbuild was terminated during `syncconfig`

Raw failure:

```text
make[2]: *** Deleting file 'include/config/auto.conf.cmd'
make[4]: *** [scripts/kconfig/Makefile:85: syncconfig] Terminated
make[3]: *** [Makefile:746: syncconfig] Terminated
make[2]: *** [/home/yunwei37/workspace/bpf-benchmark/vendor/linux-framework/Makefile:866: include/config/auto.conf.cmd] Terminated
make[1]: *** [Makefile:248: __sub-make] Terminated
Terminated
make: *** [Makefile:492: aws-x86-full] Error 143
```

Cleanup result:

- the instance still remained `running`
- I terminated it manually
- final AWS state:

```text
i-0e9ba1b33a3eed068  terminated  User initiated (2026-03-29 02:16:43 GMT)
```

## VM lock verification

I explicitly checked the relevant x86 AWS entry points with ripgrep:

- `runner/scripts/aws_x86.sh`
- `runner/scripts/x86_remote_benchmark.py`
- `Makefile`

There were no hits for:

- `with_vm_lock`
- `wrap_with_vm_lock`
- `vm-corpus`
- `vm-e2e`
- `lock_path_for_machine`

The only `aws-x86-full` wiring remains:

```make
aws-x86-full:
	"$(AWS_X86_SCRIPT)" full
```

So the remote AWS x86 flow is not blocked by local VM lock checks.

## Current blocker summary

The remaining blocker is the local x86 kernel build path used by the AWS script:

1. Earlier cached x86 artifacts were not AWS-bootable (`xfs` / `ena` / `nvme` mismatch).
2. After forcing the EC2-relevant config to `=y`, local `make modules` still proved unstable in this shared source tree:
   - one run failed in module BTF processing (`Failed to parse base BTF './vmlinux': -5`)
   - another run was terminated during `syncconfig`
3. Because those failures happened before benchmark execution, I could not produce benchmark JSON or performance numbers.
4. Because the process exited abnormally during these attempts, the intended automatic EC2 termination still did not complete in practice.

## Final state

Final AWS state is clean:

- `i-0e2f443638d5b02f8`: terminated
- `i-072609e1e575fc9fd`: terminated
- `i-0e9ba1b33a3eed068`: terminated
- `.cache/aws-x86/state/instance.env`: absent

There is still **no successful full x86 AWS benchmark result** in this session.
