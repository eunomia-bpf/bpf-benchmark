# Phase 3 Wave 3 Codex 1 Report

Date: 2026-05-10  
Branch: `main`  
Scope: clippy cleanup plus helper hoist for the small-pass BBProgram migration slice.

## Clippy Diagnostics Fixed

- `bpfopt/crates/bpfopt/src/passes/const_prop.rs:577`: fixed `clippy::type_complexity` by replacing `Option<(&mut Vec<(InsnSite, Vec<BpfInsn>)>, &mut Vec<SkipReason>)>` with `ConstPropReplacement` plus `RewriteOutputs<'_>`.
- `bpfopt/crates/bpfopt/src/passes/const_prop.rs:616`: fixed `clippy::too_many_arguments` by removing the always-`None` `frame` argument from `analyze_instruction`.
- `bpfopt/crates/bpfopt/src/passes/bulk_memory_tests.rs:53`: fixed `clippy::too_many_arguments` by narrowing the test-only `pack_memset_payload` helper to the fields actually used by the tests.
- `bpfopt/crates/bpfopt/src/pass_tests.rs:127`: fixed `clippy::unnecessary_map_or` with `.is_none_or(|states| states.is_empty())`.

## Helpers Hoisted

- `site_current_pc`: hoisted from `bulk_memory.rs`, `extract.rs`, `endian.rs`, `rotate.rs`, and `prefetch.rs` to `bpfopt/crates/bpfopt/src/analysis/bbprogram_helpers.rs`.
- `block_slot_offset`: hoisted from `bulk_memory.rs`, `extract.rs`, `endian.rs`, and `rotate.rs` to `bbprogram_helpers.rs`.
- `packet_ctx_layout`: hoisted from `skb_load_bytes.rs`, `prefetch.rs`, and `bounds_check_merge.rs` to `bbprogram_helpers.rs` with `PacketCtxLayoutScope` so skb-helper-only TC behavior stays distinct from broader packet-access behavior.
- `read_json_file<T>` and `annotations_from_profile`: hoisted from `branch_flip.rs` and `prefetch.rs` to `bbprogram_helpers.rs`.
- Register-state helpers: hoisted the shared `advance_reg_state` / ALU advance / `combine_ldimm64` logic from `bulk_memory.rs` and `skb_load_bytes.rs` to a generic `SimpleRegValue` helper in `bbprogram_helpers.rs`. `bulk_memory.rs` keeps only a small `advance_reg_state_at_site` adapter to fetch BBProgram LD_IMM64 second-slot metadata fail-fast.

Scoped diff LOC: `280` insertions, `454` deletions, net `-174` LOC.  
Scoped file LOC: `6436` before, `6262` after.

## bbprogram_helpers.rs Status

Kept. The dead `BBProgramAddressView` had no production caller and was deleted. The file now has production callers for BBProgram site/slot helpers, packet context layout selection, profile JSON/annotation helpers, and shared simple register-state simulation.

## Verification

- `cargo build --release --manifest-path bpfopt/Cargo.toml`: PASS
- `cargo test --release --manifest-path bpfopt/Cargo.toml`: PASS, `217` tests
- `cargo clippy --release --manifest-path bpfopt/Cargo.toml --all-targets -- -D dead_code -D warnings`: PASS
