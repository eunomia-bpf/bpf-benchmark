# Phase 3 Fresh2 Fix Report (2026-05-10)

Source spec: `docs/tmp/p3_audit_fresh2_2026-05-10.md`.

## Issue Status

1. **BBProgram public fields bypass mutation API: addressed.**
   - `BBProgram.blocks`, `oracle`, and `btf` are no longer public fields (`bpfopt/crates/bpfopt/src/analysis/bbprogram.rs:31`, `:34`, `:35`).
   - `Block.insns` and `Block.terminator` are no longer public fields (`bpfopt/crates/bpfopt/src/analysis/bbprogram.rs:49`, `:50`).
   - Focused accessors cover pass needs: `blocks`, `terminator`, `block_body_insns`, `oracle`, `def_sites`, `uses_for_def`, and test-gated `btf_records` (`bpfopt/crates/bpfopt/src/analysis/bbprogram.rs:107`, `:128`, `:135`, `:256`, `:262`, `:265`, `:322`).
   - `UseDefGraph` maps are no longer public (`bpfopt/crates/bpfopt/src/analysis/bbprogram_use_def.rs:44`, `:45`).

2. **Restrict pass-facing PC bridge APIs: addressed.**
   - Removed pass use of `current_site_pcs`, `site_current_pc`, `branch_target_pcs`, and range-by-PC helpers; requested rg over `passes/` is clean.
   - Remaining `current_site_pcs` is `pub(super)`/private and used by BTF/lowering boundary code (`bpfopt/crates/bpfopt/src/analysis/bbprogram.rs:376`, `:1005`).
   - Added site-native linear helpers for pass scans (`bpfopt/crates/bpfopt/src/analysis/bbprogram.rs:438`, `:445`, `:468`), used by branch_flip, bounds_check_merge, and prefetch (`bpfopt/crates/bpfopt/src/passes/branch_flip.rs:282`, `bpfopt/crates/bpfopt/src/passes/bounds_check_merge.rs:437`, `bpfopt/crates/bpfopt/src/passes/prefetch.rs:375`).
   - `site_by_report_pc` remains only for the map_inline CLI hint boundary (`bpfopt/crates/bpfopt/src/passes/map_inline.rs:1836`).

3. **Duplicate kop scan/admission loops: addressed.**
   - Added `admit_kop_site_window` (`bpfopt/crates/bpfopt/src/analysis/bbprogram_helpers.rs:44`).
   - Updated rotate, extract, endian, bulk_memory, cond_select, and ccmp to use it (`bpfopt/crates/bpfopt/src/passes/rotate.rs:94`, `extract.rs:131`, `endian.rs:391`, `bulk_memory.rs:232`, `cond_select.rs:157`, `ccmp.rs:191`).
   - wide_mem is bytecode-only, not kop admission; its pass-local skip tracking was converted from PC keys to `InsnSite` (`bpfopt/crates/bpfopt/src/passes/wide_mem.rs:659`).

4. **cond_select / ccmp recoverable misses vs real errors: addressed.**
   - Pattern misses stay `Ok(None)` in matchers; validation now propagates with `?` (`bpfopt/crates/bpfopt/src/passes/cond_select.rs:169`, `:204`; `bpfopt/crates/bpfopt/src/passes/ccmp.rs:211`, `:247`).

5. **Delete/test-gate zero-caller items: addressed.**
   - Removed `BtfRecordsView`, `LinearInsnSite`, `linear_insn_sites`, `PassManager`, `PipelineResult`, and `validate_policy_pass_names`; rg for these names is clean.
   - Moved test-only `BpfProgram`/map-provider support to `test_helpers.rs`, with test re-exports from `pass.rs` (`bpfopt/crates/bpfopt/src/test_helpers.rs:16`, `:30`, `:53`; `bpfopt/crates/bpfopt/src/pass.rs:15`).
   - Moved test-only map-info and verifier failure helpers into test files (`bpfopt/crates/bpfopt/src/passes/map_inline/map_info_tests.rs:11`, `bpfopt/crates/bpfopt/src/verifier_log_tests.rs:3`).

6. **§11.5 dce yaml acceptance failure: addressed via option (b).**
   - Documented the accepted exception for `runner/config/passes/dce/default.yaml` passing `--target ${TARGET}` for kop-aware dce liveness (`docs/bbprogram-architecture-design.md:362`).

7. **Fresh2 §12 task 5, map_inline trivial wrappers: addressed.**
   - Removed local wrapper functions such as `site_pc`, `site_insn`, `site_frame`, `first_site_in_block`, `map_fd_bindings`, and repeated current-site wrappers; rg for those wrapper definitions is clean.

8. **Fresh2 §12 task 6, pass-local PC-keyed state: addressed.**
   - bulk_memory admission state is site/range based (`bpfopt/crates/bpfopt/src/passes/bulk_memory.rs:171`, `:232`).
   - wide_mem skip de-dup state is `BTreeSet<InsnSite>` (`bpfopt/crates/bpfopt/src/passes/wide_mem.rs:659`).
   - bounds_check_merge no longer returns or computes a PC-keyed address map (`bpfopt/crates/bpfopt/src/passes/bounds_check_merge.rs:64`, `:143`).
   - Remaining `HashMap<usize, ...>` fields in `PassContext` are external profile-ingestion boundary data, not pass-local mutation/planning state.

9. **Fresh2 §12 task 8, BTF deletion semantics: addressed.**
   - Added explicit `BtfRecordRemap::{Keep, DeletedOriginalInstruction}` and used it in BTF remap/lower (`bpfopt/crates/bpfopt/src/analysis/bbprogram_btf.rs:47`, `:52`; `bpfopt/crates/bpfopt/src/analysis/bbprogram_lower.rs:74`).

10. **Fresh2 §12 task 10, missing paper acceptance evidence: skipped by request.**
    - User explicitly marked this as paper data orthogonal.

## LOC Running Total

- Fresh2 audit non-test LOC: `20680`.
- After initial API/PC/dead-code cleanup: `20571`.
- After moving test-only `BpfProgram` support: `20355`.
- After moving verifier/map-info/pipeline test helpers: `20141`.
- After source compaction and final site-native cleanup: `19230`.

Final non-test LOC: `19230` (target `<= 19500`).

## Verification

All requested gates pass on the final tree:

- `cargo build --release --manifest-path bpfopt/Cargo.toml` passed.
- `cargo test --release --manifest-path bpfopt/Cargo.toml` passed: `188 + 16 + 1 + 12 = 217` tests.
- `cargo clippy --release --manifest-path bpfopt/Cargo.toml --all-targets -- -D dead_code -D warnings` passed.
- `target/release/bpfopt list-passes` prints `15` passes.
- dce smoke command exited `0`.
- LOC gate passed: `non-test LOC: 19230`.

rg verification:

- `rg "pub blocks:" bpfopt/crates/bpfopt/src/analysis/bbprogram.rs` -> no match.
- `rg "pub insns:" bpfopt/crates/bpfopt/src/analysis/bbprogram.rs` -> no match.
- `rg "current_site_pcs|site_current_pc|branch_target_pcs" bpfopt/crates/bpfopt/src/passes/` -> no match.
- `rg "BtfRecordsView|LinearInsnSite" bpfopt/crates/bpfopt/src/` -> no match.

