diff --git a/bpf/bpf_lxc.c b/bpf/bpf_lxc.c
index 02fdeaddc2..4b898c530f 100644
--- a/bpf/bpf_lxc.c
+++ b/bpf/bpf_lxc.c
@@ -2407,7 +2407,7 @@ int tail_ipv4_to_endpoint(struct __ctx_buff *ctx)
 	__s8 ext_err = 0;
 	int ret;
 
-	if (!revalidate_data(ctx, &data, &data_end, &ip4)) {
+	if (unlikely(!revalidate_data(ctx, &data, &data_end, &ip4))) {
 		ret = DROP_INVALID;
 		goto out;
 	}
@@ -2443,20 +2443,17 @@ int tail_ipv4_to_endpoint(struct __ctx_buff *ctx)
 
 	ret = ipv4_policy(ctx, ip4, src_sec_identity, NULL, &ext_err,
 			  &proxy_port, false);
-	switch (ret) {
-	case POLICY_ACT_PROXY_REDIRECT:
-		if (!revalidate_data(ctx, &data, &data_end, &ip4)) {
+	if (likely(ret == CTX_ACT_OK))
+		goto out;
+
+	if (ret == POLICY_ACT_PROXY_REDIRECT) {
+		if (unlikely(!revalidate_data(ctx, &data, &data_end, &ip4))) {
 			ret = DROP_INVALID;
 			goto out;
 		}
 
 		ret = ctx_redirect_to_proxy_hairpin_ipv4(ctx, ip4, proxy_port);
 		ctx->mark = ctx_load_meta(ctx, CB_PROXY_MAGIC);
-		break;
-	case CTX_ACT_OK:
-		break;
-	default:
-		break;
 	}
 out:
 	if (IS_ERR(ret))
