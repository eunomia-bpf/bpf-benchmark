diff --git a/bpf/bpf_lxc.c b/bpf/bpf_lxc.c
index 02fdeaddc2..84bb65e677 100644
--- a/bpf/bpf_lxc.c
+++ b/bpf/bpf_lxc.c
@@ -2495,13 +2495,13 @@ int cil_lxc_policy(struct __ctx_buff *ctx)
 	__be16 proto;
 	int ret;
 
-	if (!validate_ethertype(ctx, &proto)) {
+	if (unlikely(!validate_ethertype(ctx, &proto))) {
 		ret = DROP_UNSUPPORTED_L2;
 		goto out;
 	}
 
 	ret = pull_l3_hdr(ctx, proto);
-	if (ret < 0)
+	if (unlikely(ret < 0))
 		goto out;
 
 	switch (proto) {
