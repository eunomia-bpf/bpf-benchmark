diff --git a/bpf/bpf_lxc.c b/bpf/bpf_lxc.c
index 02fdeaddc2..5d837a1bc9 100644
--- a/bpf/bpf_lxc.c
+++ b/bpf/bpf_lxc.c
@@ -1535,25 +1535,25 @@ static __always_inline int handle_ipv4_from_lxc(struct __ctx_buff *ctx, __u32 *d
 		}
 
 		/* Emit verdict if drop or if allow for CT_NEW. */
-		if (verdict != CTX_ACT_OK || ct_status != CT_ESTABLISHED) {
+		if (unlikely(verdict != CTX_ACT_OK || ct_status != CT_ESTABLISHED)) {
 			send_policy_verdict_notify(ctx, *dst_sec_identity, tuple->dport,
 						   tuple->nexthdr, POLICY_EGRESS, 0,
 						   verdict, proxy_port,
 						   policy_match_type, audited,
 						   auth_type, cookie);
-		}
 
-		if (verdict != CTX_ACT_OK) {
-			/* If policy_deny_response_enabled is set and the packet has been denied,
-			 * respond with an ICMPv4 "Destination Unreachable"
-			 */
-			if (CONFIG(policy_deny_response_enabled) &&
-			    (verdict == DROP_POLICY || verdict == DROP_POLICY_DENY)) {
-				ctx_store_meta(ctx, CB_VERDICT, verdict);
-				return tail_call_internal(ctx, CILIUM_CALL_IPV4_POLICY_DENIED,
-							ext_err);
+			if (verdict != CTX_ACT_OK) {
+				/* If policy_deny_response_enabled is set and the packet has been denied,
+				 * respond with an ICMPv4 "Destination Unreachable"
+				 */
+				if (CONFIG(policy_deny_response_enabled) &&
+				    (verdict == DROP_POLICY || verdict == DROP_POLICY_DENY)) {
+					ctx_store_meta(ctx, CB_VERDICT, verdict);
+					return tail_call_internal(ctx, CILIUM_CALL_IPV4_POLICY_DENIED,
+								  ext_err);
+				}
+				return verdict;
 			}
-			return verdict;
 		}
 
 		break;
@@ -2274,14 +2274,15 @@ ipv4_policy(struct __ctx_buff *ctx, struct iphdr *ip4, __u32 src_label,
 			}
 		}
 		/* Emit verdict if drop or if allow for CT_NEW. */
-		if (verdict != CTX_ACT_OK || ret != CT_ESTABLISHED)
+		if (unlikely(verdict != CTX_ACT_OK || ret != CT_ESTABLISHED)) {
 			send_policy_verdict_notify(ctx, src_label, tuple->dport,
 						   tuple->nexthdr, POLICY_INGRESS, 0,
 						   verdict, *proxy_port, policy_match_type, audited,
 						   auth_type, cookie);
 
-		if (verdict != CTX_ACT_OK)
-			return verdict;
+			if (verdict != CTX_ACT_OK)
+				return verdict;
+		}
 
 		break;
 	}
@@ -2407,13 +2408,13 @@ int tail_ipv4_to_endpoint(struct __ctx_buff *ctx)
 	__s8 ext_err = 0;
 	int ret;
 
-	if (!revalidate_data(ctx, &data, &data_end, &ip4)) {
+	if (unlikely(!revalidate_data(ctx, &data, &data_end, &ip4))) {
 		ret = DROP_INVALID;
 		goto out;
 	}
 
 	/* Packets from the proxy will already have a real identity. */
-	if (identity_is_reserved(src_sec_identity)) {
+	if (unlikely(identity_is_reserved(src_sec_identity))) {
 		const struct remote_endpoint_info *info;
 
 		info = lookup_ip4_remote_endpoint(ip4->saddr, 0);
@@ -2445,7 +2446,7 @@ int tail_ipv4_to_endpoint(struct __ctx_buff *ctx)
 			  &proxy_port, false);
 	switch (ret) {
 	case POLICY_ACT_PROXY_REDIRECT:
-		if (!revalidate_data(ctx, &data, &data_end, &ip4)) {
+		if (unlikely(!revalidate_data(ctx, &data, &data_end, &ip4))) {
 			ret = DROP_INVALID;
 			goto out;
 		}
