diff --git a/bpf/process/bpf_generic_tracepoint.c b/bpf/process/bpf_generic_tracepoint.c
index 6b51109bb..a25cc0cff 100644
--- a/bpf/process/bpf_generic_tracepoint.c
+++ b/bpf/process/bpf_generic_tracepoint.c
@@ -179,7 +179,6 @@ __attribute__((section("tracepoint/generic_tracepoint"), used)) int
 generic_tracepoint_event(struct generic_tracepoint_event_arg *ctx)
 {
 	struct msg_generic_kprobe *msg;
-	struct task_struct *task;
 	struct event_config *config;
 	int zero = 0, i;
 
@@ -250,11 +249,6 @@ generic_tracepoint_event(struct generic_tracepoint_event_arg *ctx)
 	for (i = 0; i < MAX_CONFIGURED_SELECTORS; i++)
 		msg->sel.active[i] = 0;
 	msg->sel.pass = false;
-	task = (struct task_struct *)get_current_task();
-	/* Initialize namespaces to apply filters on them */
-	get_namespaces(&msg->ns, task);
-	/* Initialize capabilities to apply filters on them */
-	get_current_subj_caps(&msg->caps, task);
 #ifdef __NS_CHANGES_FILTER
 	msg->sel.match_ns = 0;
 #endif
diff --git a/bpf/process/generic_calls.h b/bpf/process/generic_calls.h
index 932f170fc..df6d29134 100644
--- a/bpf/process/generic_calls.h
+++ b/bpf/process/generic_calls.h
@@ -31,7 +31,6 @@ generic_start_process_filter(void *ctx, struct bpf_map_def *calls)
 {
 	struct msg_generic_kprobe *msg;
 	struct event_config *config;
-	struct task_struct *task;
 	int i, zero = 0;
 
 	msg = map_lookup_elem(&process_call_heap, &zero);
@@ -58,11 +57,6 @@ generic_start_process_filter(void *ctx, struct bpf_map_def *calls)
 	msg->tailcall_index_process = 0;
 	msg->tailcall_index_selector = 0;
 	generic_path_init(msg);
-	task = (struct task_struct *)get_current_task();
-	/* Initialize namespaces to apply filters on them */
-	get_namespaces(&msg->ns, task);
-	/* Initialize capabilities to apply filters on them */
-	get_current_subj_caps(&msg->caps, task);
 #ifdef __NS_CHANGES_FILTER
 	msg->sel.match_ns = 0;
 #endif
diff --git a/bpf/process/pfilter.h b/bpf/process/pfilter.h
index 8ff9e0145..26ca9b13b 100644
--- a/bpf/process/pfilter.h
+++ b/bpf/process/pfilter.h
@@ -413,6 +413,20 @@ struct nc_filter {
  */
 #define NUM_NS_FILTERS_SMALL 4
 
+FUNC_INLINE void selector_load_namespaces(struct msg_generic_kprobe *msg)
+{
+	struct task_struct *task = (struct task_struct *)get_current_task();
+
+	get_namespaces(&msg->ns, task);
+}
+
+FUNC_INLINE void selector_load_capabilities(struct msg_generic_kprobe *msg)
+{
+	struct task_struct *task = (struct task_struct *)get_current_task();
+
+	get_current_subj_caps(&msg->caps, task);
+}
+
 FUNC_INLINE int
 selector_process_filter(__u32 *f, __u32 index, struct execve_map_value *enter,
 			struct msg_generic_kprobe *msg)
@@ -426,6 +440,7 @@ selector_process_filter(__u32 *f, __u32 index, struct execve_map_value *enter,
 	struct caps_filter *caps;
 	__u32 len;
 	__u64 i;
+	bool ns_ready = false, caps_ready = false;
 
 	/* Do binary and parent filter first for selector index */
 	if (!match_binaries(index, enter, &enter->bin))
@@ -483,32 +498,36 @@ selector_process_filter(__u32 *f, __u32 index, struct execve_map_value *enter,
 	len = *(__u32 *)((__u64)f + (index & INDEX_MASK));
 	index += 4; /* 4: ns header */
 	len -= 4;
+	if (len > 0) {
+		selector_load_namespaces(msg);
+		ns_ready = true;
 
 #ifdef __LARGE_BPF_PROG
-	for (i = 0; i < ns_max_types; i++) {
+		for (i = 0; i < ns_max_types; i++) {
 #else
 #pragma unroll
 	/* with more than 4 iterations it results in too big programs */
-	for (i = 0; i < NUM_NS_FILTERS_SMALL; i++) {
+		for (i = 0; i < NUM_NS_FILTERS_SMALL; i++) {
 #endif
-		if (len > 0) {
-			ns = (struct ns_filter *)((u64)f + (index & INDEX_MASK));
-			/* 12: namespace, op, length */
-			index += sizeof(struct ns_filter);
-			struct selector_filter sel = {
-				.index = index,
-				.ty = ns->op,
-				.flags = ns->ty,
-				.len = ns->len,
-			};
-			res = selector_match(f, &sel, enter, msg,
-					     &process_filter_namespace);
-			/* now index points at the end of namespace filter */
-			index += ((ns->len * sizeof(ns->val[0])) & VALUES_MASK);
-			len -= (sizeof(struct ns_filter) + (ns->len * sizeof(ns->val[0])));
+			if (len > 0) {
+				ns = (struct ns_filter *)((u64)f + (index & INDEX_MASK));
+				/* 12: namespace, op, length */
+				index += sizeof(struct ns_filter);
+				struct selector_filter sel = {
+					.index = index,
+					.ty = ns->op,
+					.flags = ns->ty,
+					.len = ns->len,
+				};
+				res = selector_match(f, &sel, enter, msg,
+						     &process_filter_namespace);
+				/* now index points at the end of namespace filter */
+				index += ((ns->len * sizeof(ns->val[0])) & VALUES_MASK);
+				len -= (sizeof(struct ns_filter) + (ns->len * sizeof(ns->val[0])));
+			}
+			if (res == PFILTER_REJECT)
+				return res;
 		}
-		if (res == PFILTER_REJECT)
-			return res;
 	}
 
 	/* matchCapabilities */
@@ -520,6 +539,14 @@ selector_process_filter(__u32 *f, __u32 index, struct execve_map_value *enter,
 	if (len > 0) {
 		caps = (struct caps_filter *)((u64)f + (index & INDEX_MASK));
 		index += sizeof(struct caps_filter); /* 20: ty, op, ns, val */
+		if (!ns_ready) {
+			selector_load_namespaces(msg);
+			ns_ready = true;
+		}
+		if (!caps_ready) {
+			selector_load_capabilities(msg);
+			caps_ready = true;
+		}
 		res = process_filter_capabilities(caps->ty, caps->op, caps->ns,
 						  caps->val, &msg->ns, &msg->caps);
 	}
@@ -536,6 +563,10 @@ selector_process_filter(__u32 *f, __u32 index, struct execve_map_value *enter,
 	if (len > 0) {
 		nc = (struct nc_filter *)((u64)f + (index & INDEX_MASK));
 		index += sizeof(struct nc_filter); /* 8: op, val */
+		if (!ns_ready) {
+			selector_load_namespaces(msg);
+			ns_ready = true;
+		}
 		res = process_filter_namespace_change(nc->op, nc->value, enter,
 						      &msg->ns, &msg->caps, &msg->sel);
 		/* now index points at the end of namespace change filter */
@@ -555,6 +586,14 @@ selector_process_filter(__u32 *f, __u32 index, struct execve_map_value *enter,
 	if (len > 0) {
 		caps = (struct caps_filter *)((u64)f + (index & INDEX_MASK));
 		index += sizeof(struct caps_filter); /* 20: ty, op, ns, val */
+		if (!ns_ready) {
+			selector_load_namespaces(msg);
+			ns_ready = true;
+		}
+		if (!caps_ready) {
+			selector_load_capabilities(msg);
+			caps_ready = true;
+		}
 		res = process_filter_capability_change(
 			caps->ty, caps->op, caps->ns, caps->val, &msg->ns, &msg->caps, &msg->sel);
 	}
