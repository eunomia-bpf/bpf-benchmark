diff --git a/katran/lib/bpf/balancer.c b/katran/lib/bpf/balancer.c
index 6d16faad..880fc5be 100644
--- a/katran/lib/bpf/balancer.c
+++ b/katran/lib/bpf/balancer.c
@@ -87,7 +87,8 @@ increment_ch_drop_real_0() {
 __attribute__((__always_inline__)) static inline bool get_packet_dst(
     struct real_definition** real,
     struct packet_description* pckt,
-    struct vip_meta* vip_info,
+    __u32 vip_flags,
+    __u32 vip_num,
     bool is_ipv6,
     void* lru_map) {
   // to update lru w/ new connection
@@ -102,7 +103,7 @@ __attribute__((__always_inline__)) static inline bool get_packet_dst(
   under_flood = is_under_flood(&cur_time);
 
 #ifdef LPM_SRC_LOOKUP
-  if ((vip_info->flags & F_SRC_ROUTING) && !under_flood) {
+  if ((vip_flags & F_SRC_ROUTING) && !under_flood) {
     __u32* lpm_val;
     if (is_ipv6) {
       struct v6_lpm_key lpm_key_v6 = {};
@@ -133,7 +134,7 @@ __attribute__((__always_inline__)) static inline bool get_packet_dst(
   if (!src_found) {
     bool hash_16bytes = is_ipv6;
 
-    if (vip_info->flags & F_HASH_DPORT_ONLY) {
+    if (vip_flags & F_HASH_DPORT_ONLY) {
       // service which only use dst port for hash calculation
       // e.g. if packets has same dst port -> they will go to the same real.
       // usually VoIP related services.
@@ -141,7 +142,7 @@ __attribute__((__always_inline__)) static inline bool get_packet_dst(
       memset(pckt->flow.srcv6, 0, 16);
     }
     hash = get_packet_hash(pckt, hash_16bytes) % RING_SIZE;
-    key = RING_SIZE * (vip_info->vip_num) + hash;
+    key = RING_SIZE * vip_num + hash;
 
     real_pos = bpf_map_lookup_elem(&ch_rings, &key);
     if (!real_pos) {
@@ -163,7 +164,7 @@ __attribute__((__always_inline__)) static inline bool get_packet_dst(
     increment_ch_drop_no_real();
     return false;
   }
-  if (lru_map && !(vip_info->flags & F_LRU_BYPASS) && !under_flood &&
+  if (lru_map && !(vip_flags & F_LRU_BYPASS) && !under_flood &&
       !(pckt->flags & F_RST_SET)) {
     if (pckt->flow.proto == IPPROTO_UDP) {
       new_dst_lru.atime = cur_time;
@@ -267,9 +268,7 @@ check_decap_dst(struct packet_description* pckt, bool is_ipv6, bool* pass) {
 __attribute__((__always_inline__)) static inline int perform_global_lru_lookup(
     struct real_definition** dst,
     struct packet_description* pckt,
-    __u32 cpu_num,
-    struct vip_meta* vip_info,
-    bool is_ipv6) {
+    __u32 cpu_num) {
   // lookup in the global cache
   void* g_lru_map = bpf_map_lookup_elem(&global_lru_maps, &cpu_num);
   __u32 global_lru_stats_key = MAX_VIPS + GLOBAL_LRU_CNTR;
@@ -519,7 +518,7 @@ process_udp_stable_routing(
     void* data_end,
     struct real_definition** dst,
     struct packet_description* pckt,
-    bool is_ipv6) {
+    __u64 transport_off) {
   __u32 stable_rt_stats_key = 0;
   struct lb_stable_rt_packets_stats* stable_rt_packets_stats =
       bpf_map_lookup_elem(&stable_rt_stats, &stable_rt_stats_key);
@@ -527,7 +526,7 @@ process_udp_stable_routing(
     return XDP_DROP;
   }
   struct udp_stable_rt_result usr =
-      parse_udp_stable_rt_hdr(data, data_end, is_ipv6, pckt);
+      parse_udp_stable_rt_hdr(data, data_end, transport_off);
   if (usr.server_id > 0) {
     // server_id is expected to always be positive
     __u32 key = usr.server_id;
@@ -580,7 +579,6 @@ increment_quic_cid_version_stats(
 __attribute__((__always_inline__)) static inline int update_vip_lru_miss_stats(
     struct vip_definition* vip,
     struct packet_description* pckt,
-    struct vip_meta* vip_info,
     bool is_ipv6) {
   // track the lru miss counter of vip in vip_miss_stats
   __u32 vip_miss_stats_key = 0;
@@ -653,11 +651,11 @@ incr_server_id_routing_stats(__u32 vip_num, bool newConn, bool misMatchInLRU) {
 __attribute__((__always_inline__)) static inline int check_udp_flow_migration(
     struct real_definition** dst,
     struct packet_description* pckt,
-    struct vip_meta* vip_info,
+    __u32 vip_flags,
     struct vip_definition* vip) {
   __u64 cur_time;
-  if (dst && pckt->flow.proto == IPPROTO_UDP &&
-      vip_info->flags & F_UDP_FLOW_MIGRATION && !is_under_flood(&cur_time)) {
+  if (*dst && pckt->flow.proto == IPPROTO_UDP &&
+      vip_flags & F_UDP_FLOW_MIGRATION && !is_under_flood(&cur_time)) {
     // Check if the real is down using the vip_to_down_reals_map
     void* down_reals_map = bpf_map_lookup_elem(&vip_to_down_reals_map, vip);
     if (down_reals_map) {
@@ -706,6 +704,8 @@ process_packet(struct xdp_md* xdp, __u64 nh_off, bool is_ipv6) {
     return action;
   }
   protocol = pckt.flow.proto;
+  bool is_icmp = pckt.flags & F_ICMP;
+  __u64 transport_off = is_icmp ? calc_offset(is_ipv6, true) : th_off;
 
 #ifdef INLINE_DECAP_IPIP
   /* This is to workaround a verifier issue for 5.2.
@@ -757,7 +757,7 @@ process_packet(struct xdp_md* xdp, __u64 nh_off, bool is_ipv6) {
       return XDP_DROP;
     }
   } else if (protocol == IPPROTO_UDP) {
-    if (!parse_udp(data, data_end, is_ipv6, &pckt)) {
+    if (!parse_udp_at(data, data_end, transport_off, &pckt)) {
       return XDP_DROP;
     }
 #ifdef INLINE_DECAP_GUE
@@ -800,6 +800,7 @@ process_packet(struct xdp_md* xdp, __u64 nh_off, bool is_ipv6) {
     }
   }
 
+  __u32 vip_flags = vip_info->flags;
   if (data_end - data > MAX_PCKT_SIZE) {
     REPORT_PACKET_TOOBIG(xdp, data, data_end - data, false);
 #ifdef ICMP_TOOBIG_GENERATION
@@ -828,7 +829,7 @@ process_packet(struct xdp_md* xdp, __u64 nh_off, bool is_ipv6) {
   // total packets
   data_stats->v1 += 1;
 
-  if ((vip_info->flags & F_HASH_NO_SRC_PORT)) {
+  if ((vip_flags & F_HASH_NO_SRC_PORT)) {
     // service, where diff src port, but same ip must go to the same real,
     // e.g. gfs
     pckt.flow.port16[0] = 0;
@@ -851,7 +852,7 @@ process_packet(struct xdp_md* xdp, __u64 nh_off, bool is_ipv6) {
   }
 
   // Lookup dst based on id in packet
-  if ((vip_info->flags & F_QUIC_VIP)) {
+  if ((vip_flags & F_QUIC_VIP)) {
     bool is_icmp = (pckt.flags & F_ICMP);
     if (is_icmp) {
       // as per rfc792, the "Destination Unreachable Message" has the internet
@@ -877,7 +878,7 @@ process_packet(struct xdp_md* xdp, __u64 nh_off, bool is_ipv6) {
       if (!quic_packets_stats) {
         return XDP_DROP;
       }
-      struct quic_parse_result qpr = parse_quic(data, data_end, is_ipv6, &pckt);
+      struct quic_parse_result qpr = parse_quic(data, data_end, transport_off);
       if (qpr.server_id > 0) {
         // server_id is expected to always be positive. get a server id from
         // quic packet
@@ -934,8 +935,8 @@ process_packet(struct xdp_md* xdp, __u64 nh_off, bool is_ipv6) {
   }
 #ifdef UDP_STABLE_ROUTING
   if (pckt.flow.proto == IPPROTO_UDP &&
-      vip_info->flags & F_UDP_STABLE_ROUTING_VIP) {
-    process_udp_stable_routing(data, data_end, &dst, &pckt, is_ipv6);
+      vip_flags & F_UDP_STABLE_ROUTING_VIP) {
+    process_udp_stable_routing(data, data_end, &dst, &pckt, transport_off);
   }
 #endif // UDP_STABLE_ROUTING
 
@@ -964,7 +965,7 @@ process_packet(struct xdp_md* xdp, __u64 nh_off, bool is_ipv6) {
           tpr_packets_stats->ch_routed += 1;
         } else {
           // update this routing decision in the lru_map as well
-          if (lru_map && !(vip_info->flags & F_LRU_BYPASS)) {
+          if (lru_map && !(vip_flags & F_LRU_BYPASS)) {
             int res = check_and_update_real_index_in_lru(&pckt, lru_map);
             if (res == DST_MISMATCH_IN_LRU) {
               tpr_packets_stats->dst_mismatch_in_lru += 1;
@@ -980,21 +981,21 @@ process_packet(struct xdp_md* xdp, __u64 nh_off, bool is_ipv6) {
 
     // Next, try to lookup dst in the lru_cache
     if (!dst && !(pckt.flags & F_SYN_SET) &&
-        !(vip_info->flags & F_LRU_BYPASS)) {
+        !(vip_flags & F_LRU_BYPASS)) {
       connection_table_lookup(&dst, &pckt, lru_map, /*isGlobalLru=*/false);
     }
 
 #ifdef GLOBAL_LRU_LOOKUP
-    if (!dst && !(pckt.flags & F_SYN_SET) && vip_info->flags & F_GLOBAL_LRU) {
+    if (!dst && !(pckt.flags & F_SYN_SET) && vip_flags & F_GLOBAL_LRU) {
       int global_lru_lookup_result =
-          perform_global_lru_lookup(&dst, &pckt, cpu_num, vip_info, is_ipv6);
+          perform_global_lru_lookup(&dst, &pckt, cpu_num);
       if (global_lru_lookup_result >= 0) {
         return global_lru_lookup_result;
       }
     }
 #endif // GLOBAL_LRU_LOOKUP
 
-    check_udp_flow_migration(&dst, &pckt, vip_info, &vip);
+    check_udp_flow_migration(&dst, &pckt, vip_flags, &vip);
 
     // if dst is not found, route via consistent-hashing of the flow.
     if (!dst) {
@@ -1016,12 +1017,12 @@ process_packet(struct xdp_md* xdp, __u64 nh_off, bool is_ipv6) {
           lru_stats->v2 += 1;
         }
       }
-      if (!get_packet_dst(&dst, &pckt, vip_info, is_ipv6, lru_map)) {
+      if (!get_packet_dst(&dst, &pckt, vip_flags, vip_num, is_ipv6, lru_map)) {
         return XDP_DROP;
       }
 
       // track the lru miss counter of vip in vip_miss_stats
-      if (update_vip_lru_miss_stats(&vip, &pckt, vip_info, is_ipv6) >= 0) {
+      if (update_vip_lru_miss_stats(&vip, &pckt, is_ipv6) >= 0) {
         return XDP_DROP;
       }
 
@@ -1051,7 +1052,7 @@ process_packet(struct xdp_md* xdp, __u64 nh_off, bool is_ipv6) {
   data_stats->v1 += 1;
   data_stats->v2 += pkt_bytes;
 #ifdef LOCAL_DELIVERY_OPTIMIZATION
-  if ((vip_info->flags & F_LOCAL_VIP) && (dst->flags & F_LOCAL_REAL)) {
+  if ((vip_flags & F_LOCAL_VIP) && (dst->flags & F_LOCAL_REAL)) {
     return XDP_PASS;
   }
 #endif
diff --git a/katran/lib/bpf/pckt_parsing.h b/katran/lib/bpf/pckt_parsing.h
index 7ea09eed..18be3b7e 100644
--- a/katran/lib/bpf/pckt_parsing.h
+++ b/katran/lib/bpf/pckt_parsing.h
@@ -88,13 +88,12 @@ __attribute__((__always_inline__)) static inline __u64 calc_offset(
   return off;
 }
 
-__attribute__((__always_inline__)) static inline bool parse_udp(
+__attribute__((__always_inline__)) static inline bool parse_udp_at(
     void* data,
     void* data_end,
-    bool is_ipv6,
+    __u64 off,
     struct packet_description* pckt) {
   bool is_icmp = !((pckt->flags & F_ICMP) == 0);
-  __u64 off = calc_offset(is_ipv6, is_icmp);
   struct udphdr* udp;
   udp = data + off;
 
@@ -114,6 +113,15 @@ __attribute__((__always_inline__)) static inline bool parse_udp(
   return true;
 }
 
+__attribute__((__always_inline__)) static inline bool parse_udp(
+    void* data,
+    void* data_end,
+    bool is_ipv6,
+    struct packet_description* pckt) {
+  bool is_icmp = !((pckt->flags & F_ICMP) == 0);
+  return parse_udp_at(data, data_end, calc_offset(is_ipv6, is_icmp), pckt);
+}
+
 __attribute__((__always_inline__)) static inline bool parse_tcp(
     void* data,
     void* data_end,
@@ -343,8 +351,7 @@ __attribute__((__always_inline__)) static inline struct quic_parse_result
 parse_quic(
     void* data,
     void* data_end,
-    bool is_ipv6,
-    struct packet_description* pckt) {
+    __u64 off) {
   struct quic_parse_result result = {
       .server_id = FURTHER_PROCESSING,
       // initialize cid_version to 0xFF instead of 0 cause 0 is also a possible
@@ -352,8 +359,6 @@ parse_quic(
       .cid_version = 0xFF,
       .is_initial = false};
 
-  bool is_icmp = (pckt->flags & F_ICMP);
-  __u64 off = calc_offset(is_ipv6, is_icmp);
   // offset points to the beginning of transport header (udp) of quic's packet
   /*                                      |QUIC PKT TYPE|           */
   if ((data + off + sizeof(struct udphdr) + sizeof(__u8)) > data_end) {
@@ -419,13 +424,10 @@ __attribute__((__always_inline__)) static inline struct udp_stable_rt_result
 parse_udp_stable_rt_hdr(
     void* data,
     void* data_end,
-    bool is_ipv6,
-    struct packet_description* pckt) {
+    __u64 off) {
   struct udp_stable_rt_result result = {
       .server_id = STABLE_RT_NO_SERVER_ID, .is_stable_rt_pkt = false};
 
-  bool is_icmp = (pckt->flags & F_ICMP);
-  __u64 off = calc_offset(is_ipv6, is_icmp);
   // offset points to the beginning of transport header (udp)
   /*                                      |PKT TYPE|           */
   if ((data + off + sizeof(struct udphdr) + sizeof(__u8)) > data_end) {
