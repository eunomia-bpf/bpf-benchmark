diff --git a/support/ebpf/bpfdefs.h b/support/ebpf/bpfdefs.h
index ede0255..5921e0b 100644
--- a/support/ebpf/bpfdefs.h
+++ b/support/ebpf/bpfdefs.h
@@ -15,6 +15,8 @@ extern u32 with_debug_output;
 #define __type(name, val)  typeof(val) *name
 #define __array(name, val) typeof(val) *name[]
 
+#define OPTI_UNLIKELY(x) __builtin_expect(!!(x), 0)
+
 #if defined(TESTING_COREDUMP)
 
   // BPF_RODATA_VAR declares a global variable in the .rodata section,
diff --git a/support/ebpf/native_stack_trace.ebpf.c b/support/ebpf/native_stack_trace.ebpf.c
index 29e650a..8667eea 100644
--- a/support/ebpf/native_stack_trace.ebpf.c
+++ b/support/ebpf/native_stack_trace.ebpf.c
@@ -121,7 +121,7 @@ static EBPF_INLINE bool bsearch_step(void *inner_map, u32 *lo, u32 *hi, u16 page
 {
   u32 pivot         = (*lo + *hi) >> 1;
   StackDelta *delta = bpf_map_lookup_elem(inner_map, &pivot);
-  if (!delta) {
+  if (OPTI_UNLIKELY(!delta)) {
     *hi = 0;
     return false;
   }
@@ -171,7 +171,7 @@ static EBPF_INLINE ErrorCode get_stack_delta(UnwindState *state, int *addrDiff,
     (unsigned long)state->text_section_id,
     (unsigned long)state->text_section_offset);
   StackDeltaPageInfo *info = bpf_map_lookup_elem(&stack_delta_page_to_info, &key);
-  if (!info) {
+  if (OPTI_UNLIKELY(!info)) {
     DEBUG_PRINT(
       "Failure to look up stack delta page fileID %lx, page %lx",
       (unsigned long)key.fileID,
@@ -181,7 +181,7 @@ static EBPF_INLINE ErrorCode get_stack_delta(UnwindState *state, int *addrDiff,
   }
 
   void *outer_map = get_stack_delta_map(info->mapID);
-  if (!outer_map) {
+  if (OPTI_UNLIKELY(!outer_map)) {
     DEBUG_PRINT(
       "Failure to look up outer map for text section %lx in mapID %d",
       (unsigned long)exe_id,
@@ -191,7 +191,7 @@ static EBPF_INLINE ErrorCode get_stack_delta(UnwindState *state, int *addrDiff,
   }
 
   void *inner_map = bpf_map_lookup_elem(outer_map, &exe_id);
-  if (!inner_map) {
+  if (OPTI_UNLIKELY(!inner_map)) {
     DEBUG_PRINT("Failure to look up inner map for text section %lx", (unsigned long)exe_id);
     state->error_metric = metricID_UnwindNativeErrLookupStackDeltaInnerMap;
     return ERR_NATIVE_LOOKUP_STACK_DELTA_INNER_MAP;
@@ -220,7 +220,7 @@ static EBPF_INLINE ErrorCode get_stack_delta(UnwindState *state, int *addrDiff,
         break;
       }
     }
-    if (i >= 16 || hi == 0) {
+    if (OPTI_UNLIKELY(i >= 16 || hi == 0)) {
       DEBUG_PRINT("Failed bsearch in 16 steps. Corrupt data?");
       state->error_metric = metricID_UnwindNativeErrLookupIterations;
       return ERR_NATIVE_EXCEEDED_DELTA_LOOKUP_ITERATIONS;
@@ -236,7 +236,7 @@ static EBPF_INLINE ErrorCode get_stack_delta(UnwindState *state, int *addrDiff,
   idx--;
 
   StackDelta *delta = bpf_map_lookup_elem(inner_map, &idx);
-  if (!delta) {
+  if (OPTI_UNLIKELY(!delta)) {
     state->error_metric = metricID_UnwindNativeErrLookupRange;
     return ERR_NATIVE_LOOKUP_RANGE;
   }
@@ -256,7 +256,7 @@ static EBPF_INLINE ErrorCode get_stack_delta(UnwindState *state, int *addrDiff,
   *addrDiff   = deltaOffset;
   *unwindInfo = delta->unwindInfo;
 
-  if (delta->unwindInfo == STACK_DELTA_INVALID) {
+  if (OPTI_UNLIKELY(delta->unwindInfo == STACK_DELTA_INVALID)) {
     state->error_metric = metricID_UnwindNativeErrStackDeltaInvalid;
     return ERR_NATIVE_STACK_DELTA_INVALID;
   }
@@ -392,7 +392,7 @@ static EBPF_INLINE ErrorCode unwind_one_frame(UnwindState *state, bool *stop)
   // The relevant executable is compiled with frame pointer omission, so
   // stack deltas need to be retrieved from the relevant map.
   ErrorCode error = get_stack_delta(state, &addrDiff, &unwindInfo);
-  if (error) {
+  if (OPTI_UNLIKELY(error)) {
     return error;
   }
 
@@ -435,7 +435,7 @@ static EBPF_INLINE ErrorCode unwind_one_frame(UnwindState *state, bool *stop)
     }
   } else {
     UnwindInfo *info = bpf_map_lookup_elem(&unwind_info_array, &unwindInfo);
-    if (!info) {
+    if (OPTI_UNLIKELY(!info)) {
       increment_metric(metricID_UnwindNativeErrBadUnwindInfoIndex);
       return ERR_NATIVE_BAD_UNWIND_INFO_INDEX;
     }
@@ -462,7 +462,8 @@ static EBPF_INLINE ErrorCode unwind_one_frame(UnwindState *state, bool *stop)
     }
   }
 
-  if (!cfa || bpf_probe_read_user(&state->pc, sizeof(state->pc), (void *)(cfa - 8))) {
+  if (OPTI_UNLIKELY(
+        !cfa || bpf_probe_read_user(&state->pc, sizeof(state->pc), (void *)(cfa - 8)))) {
   err_native_pc_read:
     increment_metric(metricID_UnwindNativeErrPCRead);
     return ERR_NATIVE_PC_READ;
@@ -607,7 +608,7 @@ frame_ok:
 static EBPF_INLINE int unwind_native(struct pt_regs *ctx)
 {
   PerCPURecord *record = get_per_cpu_record();
-  if (!record)
+  if (OPTI_UNLIKELY(!record))
     return -1;
 
   Trace *trace = &record->trace;
@@ -633,7 +634,7 @@ static EBPF_INLINE int unwind_native(struct pt_regs *ctx)
       record->state.text_section_id,
       record->state.text_section_offset,
       record->state.return_address);
-    if (error) {
+    if (OPTI_UNLIKELY(error)) {
       DEBUG_PRINT("failed to push native frame");
       break;
     }
@@ -641,7 +642,7 @@ static EBPF_INLINE int unwind_native(struct pt_regs *ctx)
     // Unwind the native frame using stack deltas. Stop if no next frame.
     bool stop;
     error = unwind_one_frame(&record->state, &stop);
-    if (error || stop) {
+    if (OPTI_UNLIKELY(error || stop)) {
       break;
     }
 
@@ -649,7 +650,7 @@ static EBPF_INLINE int unwind_native(struct pt_regs *ctx)
     DEBUG_PRINT(
       " pc: %llx sp: %llx fp: %llx", record->state.pc, record->state.sp, record->state.fp);
     error = get_next_unwinder_after_native_frame(record, &unwinder);
-    if (error || unwinder != PROG_UNWIND_NATIVE) {
+    if (OPTI_UNLIKELY(error || unwinder != PROG_UNWIND_NATIVE)) {
       break;
     }
   }
diff --git a/support/ebpf/tracemgmt.h b/support/ebpf/tracemgmt.h
index 7a30122..aef2b20 100644
--- a/support/ebpf/tracemgmt.h
+++ b/support/ebpf/tracemgmt.h
@@ -435,7 +435,7 @@ static inline EBPF_INLINE ErrorCode resolve_unwind_mapping(PerCPURecord *record,
 
   // Check if we have the data for this virtual address
   PIDPageMappingInfo *val = bpf_map_lookup_elem(&pid_page_to_mapping_info, &key);
-  if (!val) {
+  if (OPTI_UNLIKELY(!val)) {
     DEBUG_PRINT("Failure to look up interval memory mapping for PC 0x%lx", (unsigned long)pc);
     state->error_metric = metricID_UnwindNativeErrWrongTextSection;
     return ERR_NATIVE_NO_PID_PAGE_MAPPING;
@@ -507,7 +507,7 @@ get_next_unwinder_after_native_frame(PerCPURecord *record, int *unwinder)
 
   DEBUG_PRINT("==== Resolve next frame unwinder: frame %d ====", record->trace.stack_len);
   ErrorCode error = resolve_unwind_mapping(record, unwinder);
-  if (error) {
+  if (OPTI_UNLIKELY(error)) {
     return error;
   }
 
@@ -729,7 +729,7 @@ static inline EBPF_INLINE int collect_trace(
   // variables used to maintain state.
   DEBUG_PRINT("Resetting CPU record");
   PerCPURecord *record = get_pristine_per_cpu_record();
-  if (!record) {
+  if (OPTI_UNLIKELY(!record)) {
     return -1;
   }
 
@@ -747,7 +747,7 @@ static inline EBPF_INLINE int collect_trace(
   trace->kernel_stack_id = bpf_get_stackid(ctx, &kernel_stackmap, BPF_F_REUSE_STACKID);
   DEBUG_PRINT("kernel stack id = %d", trace->kernel_stack_id);
 
-  if (pid == 0) {
+  if (OPTI_UNLIKELY(pid == 0)) {
     tail_call(ctx, PROG_UNWIND_STOP);
     return 0;
   }
@@ -756,11 +756,11 @@ static inline EBPF_INLINE int collect_trace(
   int unwinder           = PROG_UNWIND_STOP;
   bool has_usermode_regs = false;
   ErrorCode error        = get_usermode_regs(ctx, &record->state, &has_usermode_regs);
-  if (error || !has_usermode_regs) {
+  if (OPTI_UNLIKELY(error || !has_usermode_regs)) {
     goto exit;
   }
 
-  if (!pid_information_exists(pid)) {
+  if (OPTI_UNLIKELY(!pid_information_exists(pid))) {
     u64 pid_tgid = (u64)pid << 32 | tid;
     if (report_pid(ctx, pid_tgid, RATELIMIT_ACTION_DEFAULT)) {
       increment_metric(metricID_NumProcNew);
