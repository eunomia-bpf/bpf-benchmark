{
  "cmd": "execute_plan",
  "steps": [
    {
      "command": "profile=\"${BPFREJIT_IMAGE_WORKSPACE}/runner/config/passes/hot_region_version/profiles/cilium/hash-${PROG_HASH}.json\"; bpfopt --pass hot_region_version --input \"${INPUT}\" --output \"${OUTPUT}\" --report \"${REPORT}\" --prog-type \"${PROG_TYPE}\" -- --profile \"${profile}\" --program-hash \"${PROG_HASH}\"",
      "log_level": 1,
      "name": "hot_region_version"
    }
  ]
}
