[package]
name = "bpfopt-core"
version = "0.1.0"
edition = "2021"
description = "BPF bytecode optimization passes — core library (workspace-internal)"

[dependencies]
anyhow.workspace = true
serde.workspace = true
serde_json.workspace = true
goblin.workspace = true
