# Core dependency — required for benchmark orchestration (benchmark_catalog.py, policy_utils.py, etc.)
pyyaml>=6.0

# Required for corpus ELF analysis (directive_census.py, analyze_bytecode.py, generate_default_policies.py)
pyelftools>=0.29

# Required for figure generation and rigorous statistical tests (generate_figures.py, _driver_impl_run_rigorous.py)
matplotlib>=3.7
numpy>=1.24
scipy>=1.10
