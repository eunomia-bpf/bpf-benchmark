# Minimal YAML subset understood by bpf-jit-scanner.

default_rotate: rorx
default_cmov: branch
default_wide_mem: wide
default_lea: lea

overrides:
  - program: "log2_fold*"
    cmov: branch
  - program: "cmov_select*"
    cmov: cmovcc
