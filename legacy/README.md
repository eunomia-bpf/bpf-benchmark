# Legacy

`legacy/` contains inactive material preserved for reference:

- `user_bpf_benchmark/`: the older multi-runtime userspace benchmark suite
- `third-party/`: external benchmark project snapshots present in this checkout

These directories are not part of the active three-layer workflow and are not maintained as the primary benchmark path anymore.
