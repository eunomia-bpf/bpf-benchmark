# virtme-ng exports XDG_RUNTIME_DIR=/run/user/0 into its root guest.  The guest
# runs systemd-tmpfiles before the benchmark script, so create the directory in
# the guest's private /run rather than only in the host container.
d /run/user/0 0700 root root -
