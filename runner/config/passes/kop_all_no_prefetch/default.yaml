# Single-pass kop ablation with every bytecode family except prefetch forced.
log_level: 1
command: |
  bpfopt --pass kop --input ${INPUT} --output ${OUTPUT} --report ${REPORT} --prog-type ${PROG_TYPE} --target ${TARGET} -- --bytecode-kop-mode all=force,prefetch=disable
