# Default per-pass step config for ccmp.
# Read by runner/libs/pass_config.py at execute_plan build time.
# Daemon substitutes ${...} vars; runner sends this command verbatim.
log_level: 1
capture_workdir: on_failure
kinsns:
  - name: bpf_ccmp64
    aliases:
      - bpf_ccmp64
command: |
  timeout 6000 bpfopt --pass ccmp --input ${INPUT} --output ${OUTPUT} --report ${REPORT} --prog-type ${PROG_TYPE} --target ${TARGET} -- ${PASS_LOCAL_ARGS}
