-- Generated by generate_x86_cond_spec.py from x86_cond_spec.json.
import Std

namespace KProgFormal.GeneratedX86Cond

inductive Cond
  | o
  | no
  | b
  | ae
  | e
  | ne
  | be
  | a
  | s
  | ns
  | l
  | ge
  | le
  | g
  deriving DecidableEq, Repr

def eval (cf zf sf of : Bool) : Cond -> Bool
  | .o => of
  | .no => (!of)
  | .b => cf
  | .ae => (!cf)
  | .e => zf
  | .ne => (!zf)
  | .be => (cf || zf)
  | .a => ((!cf) && (!zf))
  | .s => sf
  | .ns => (!sf)
  | .l => (sf != of)
  | .ge => (sf == of)
  | .le => (zf || (sf != of))
  | .g => ((!zf) && (sf == of))

end KProgFormal.GeneratedX86Cond
